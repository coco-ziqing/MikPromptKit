# PLAN Phase35.3 UX 优化方案 — 文件管家

> 基于 PLAN_Phase35_3_device_index.md 的用户体验重设计
> 生成：2026-07-16 | 状态：待确认后进入开发

---

## 一、命名字典（技术名 → 用户看到的名）

| 技术内部名 | 用户看到的名 | 原因 |
|-----------|-------------|------|
| 设备盘索引 | **文件管家** | "管家"=帮你看管文件，直觉秒懂 |
| Agent / pk_agent.exe | **文件管家助手** | "助手"暗示轻量无害 |
| 监控路径白名单 | **关注的文件夹** | 日常用词，不让人紧张 |
| L0 索引层 | （完全隐藏） | 实现细节 |
| L1 备份层 | **重要文件自动备份** | 直接说功能 |
| 配对码注册 | **连接这台电脑** | 类似连WiFi的直觉 |
| sha256指纹 | （不显示） | 实现细节 |
| 归档到asset_catalog | **加入项目资产库** | 复用已有概念 |
| state:new | 🆕 **新文件** | |
| state:changed | ✏️ **已修改** | |
| state:missing | ⚠️ **找不到了** | |
| state:indexed | （无标签，正常态） | |
| state:archived | 显示项目名标签 | |

---

## 二、概念压缩：7 层 → 2 层

```
优化前（7层，用户心智负担重）：
  设备列表 → 设备详情 → 监控路径列表 → 文件索引(5状态)
  → 归档按钮(选项目/模块) → 备份任务队列 → 完整性自检报告

优化后（2层，类文件管理器体验）：
  ┌─ 第一层：我的电脑列表
  │   每台连线设备一张卡片：电脑名 | 在线灯 | 关注了几
  │   个文件夹 | 有多少文件 | 有没有问题
  │
  └─ 第二层：点进某台电脑 → 按文件夹浏览文件
      每个文件：图标+名字+大小+状态标签 + "加入项目"按钮
```

用户 mental model = **"我的文件在哪台电脑上"** — 像文件管理器一样浏览，不需要学新概念。

---

## 三、功能合并（3 个混淆概念 → 2 个用户动作）

| 后台做的事 | 用户看到 | 用户操作 |
|-----------|---------|---------|
| 文件索引（scan→fingerprint→上报） | **文件快照**（自动完成，用户无感，不展示"索引"二字） | 无需操作 |
| 归档（→asset_catalog） | **加入项目资产库**（由用户主动决定哪些文件进项目） | 拖拽或点击 |
| 备份（→L1 内容寻址落盘） | **自动备份**（开关式，开启后全自动） | 开/关 |

---

## 四、核心交互流程

### 4.1 连接新电脑（配对）— 目标 ≤30 秒

```
步骤 1：WebUI 文件管家面板 → 点击「＋连接新电脑」
         ↓
步骤 2：弹出连接向导，第1步展示二维码
        二维码内容：http://192.168.0.101:8080/connect?code=AB12CD
        下方同时显示 6 位配对码（备用手动输入）
        下方显示「文件管家助手.exe」下载按钮
         ↓
步骤 3：在新电脑上操作：
        ① 浏览器打开二维码中的地址（或直接输入 IP:8080/connect）
        ② 或者在已下载的「文件管家助手.exe」中确认连接
        ③ 连接成功后 WebUI 自动跳转下一步
         ↓
步骤 4：选择关注的文件夹（此步不可跳过，不可自动填充）
        
        ┌─ 选择要关注的文件夹 ─────────────────────────┐
        │                                              │
        │  请指定需要纳入管理的文件夹：                    │
        │                                              │
        │  ┌──────────────────────────────────────┐    │
        │  │ （空列表 — 至少添加 1 个文件夹才能继续） │    │
        │  │                                      │    │
        │  │ [+ 添加文件夹]                         │    │
        │  └──────────────────────────────────────┘    │
        │                                              │
        │  💡 只扫描你指定的文件夹，未被指定              │
        │     的文件不在管理范围内，不受任何影响。         │
        └──────────────────────────────────────────────┘
        
        添加文件夹 → 弹出系统文件选择器（showDirectoryPicker 或 Agent 端浏览）
        
        ┌─ 选择要关注的文件夹 ─────────────────────────┐
        │                                              │
        │  📁 D:\Projects\特种兵学校                 ✕  │
        │  📁 D:\Projects\宣传片                     ✕  │
        │                                              │
        │  [+ 添加文件夹]                               │
        │                                              │
        │         [开始管理]  ← 添加至少1个后按钮亮起     │
        └──────────────────────────────────────────────┘
        
         ↓
步骤 5：完成。卡片出现在设备列表中，Agent 开始首次扫描。
        通知：「D:\Projects 正在扫描中...」
        扫描完成 → 通知：「发现 47 个文件」
```

### 4.2 日常使用 — 通知驱动，而非面板巡检

```
用户日常不需要打开文件管家面板。

通知机制（铃铛图标）：
  🆕 「D:\Projects 发现了 8 个新文件」→ 点击跳转到对应文件夹视图
  ✏️ 「poster.psd 内容已变更」
  ⚠️ 「scene01.c4d 原始文件找不到了」→ 红色告警
  ✅ 「3 个文件的自动备份已完成」
  
用户何时打开面板？
  → 收到通知时点进去看
  → 想把新文件加入某个项目时
  → 想添加/移除关注的文件夹时
```

### 4.3 文件加入项目 — 拖拽式，一步完成

```
浏览 D:\Projects → 看到新文件：

  🎬 scene02.c4d  96MB  今天  🆕新文件  [拖到此处加入项目▼]
                                                    │
  拖拽到侧边项目名              ┌─── 特种兵学校       │
  或者：勾选多个文件 → 批量「加入项目X」│   宣传片             │
                              └─────────────────────┘
  
  批量模式：
  ☑ scene02.c4d  96MB
  ☑ shot03.c4d   112MB
  ☑ env_map.hdr  45MB
  ─────────────────────
  已选 3 个 → [加入项目 ▼] [开启自动备份]
```

---

## 五、前端 UI 设计

### 5.1 入口

顶部导航栏：📁 **文件管家**（与「📦 项目资产」并列或在其子项）

### 5.2 设备列表（第一层）

```
┌────────────── 文件管家 ────────────────── [+ 连接新电脑] ┐
│                                                         │
│  ┌── 我的台式机 ─────────────────────────────────────┐   │
│  │  ● 在线 · 2分钟前          [3 个文件夹] [127 文件]  │   │
│  │  📁 D:\Projects (89)   📁 E:\素材 (38)            │   │
│  │  备份：12/15 已备份     [查看文件 →]               │   │
│  └──────────────────────────────────────────────────┘   │
│                                                         │
│  ┌── 渲染工作站 ─────────────────────────────────────┐   │
│  │  ○ 离线 · 3小时前         [1 个文件夹] [45 文件]    │   │
│  │  📁 F:\Render\ (45)                              │   │
│  │  备份：0/0（未开启自动备份）  [查看文件 →]          │   │
│  └──────────────────────────────────────────────────┘   │
│                                                         │
│  ┌── MacBook Pro ───────────────────────────────────┐   │
│  │  ● 在线 · 刚刚            [2 个文件夹] [213 文件]   │   │
│  │  📁 /Users/ASUS/Videos (156)                     │   │
│  │  📁 /Users/ASUS/Projects (57)                    │   │
│  │  ⚠ 1 个文件找不到了    备份：20/20 已备份          │   │
│  │  [查看文件 →]                                     │   │
│  └──────────────────────────────────────────────────┘   │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 5.3 文件浏览（第二层）— 类文件管理器视图

```
┌── 我的台式机 · D:\Projects ─────────────────────────────┐
│  [← 返回设备列表]                                        │
│                                                        │
│  筛选：[全部] [🆕新文件 8] [✏️已修改 3] [⚠️找不到 1]     │
│  排序：时间↓ / 名字 / 大小                                │
│                                                        │
│  ┌──────────────────────────────────────────────────┐  │
│  │ 🎬 scene01.c4d    128MB  2026-07-15               │  │
│  │    [特种兵学校] · 角色设定                          │  │
│  │                                              ☰   │  │
│  ├──────────────────────────────────────────────────┤  │
│  │ 🎬 scene02.c4d    96MB   2026-07-16               │  │
│  │   🆕 新文件                                  ☰   │  │
│  │   [加入项目 ▼]                                     │  │
│  ├──────────────────────────────────────────────────┤  │
│  │ 🖼️ poster.psd     45MB   2026-07-13               │  │
│  │   ✏️ 已修改                                   ☰   │  │
│  │   [加入项目 ▼]                                     │  │
│  ├──────────────────────────────────────────────────┤  │
│  │ 🎵 bgm_test.mp3   8MB    2026-07-10               │  │
│  │   ⚠️ 原文件找不到了                            ☰   │  │
│  │   提示：该文件可能已被移动或删除                      │  │
│  ├──────────────────────────────────────────────────┤  │
│  │ 🎬 hero_model.c4d 256MB  2026-07-08               │  │
│  │   🔒 已自动备份                                    │  │
│  │   [特种兵学校] · 角色模型                        ☰   │  │
│  └──────────────────────────────────────────────────┘  │
│                                                        │
│  [全选] [加入项目 ▼] [开启自动备份] [移除选中]           │
│                                                        │
│  ─────────────────────────────────────────────────────  │
│  📁 当前关注 3 个文件夹     [+ 添加]                     │
│  📊 自动备份：12/15 个重要文件已备份                     │
│  ⚙ 设置                                 [移除电脑]     │
└────────────────────────────────────────────────────────┘
```

### 5.4 右键菜单（☰ 按钮）

```
  scene01.c4d
    ├── 加入项目资产库 →
    │     ├── 特种兵学校 · 角色设定
    │     ├── 特种兵学校 · 场景
    │     ├── 宣传片 · 素材
    │     └── 新建项目...
    ├── 开启自动备份
    ├── 在文件夹中定位（Agent 端打开资源管理器）
    └── 从列表中移除（不删除文件，只是不管它了）
```

---

## 六、前端文件结构

```
frontend/static/js/file_steward_ui.js   # 自包含模块，PK_FILESTEWARD
                                        # 设备列表 + 文件浏览 + 连接向导
frontend/static/css/file_steward.css    # 样式（深色/浅色双模式）
```

index.html 注入点：导航栏加「📁 文件管家」按钮 + 主面板 `#fileStewardPanel`

---

## 七、Agent 辅助程序（文件管家助手）

### 7.1 文件命名

| 平台 | 文件名 | 大小 |
|------|--------|------|
| Windows | `文件管家助手.exe` | ≈8MB（PyInstaller onefile） |
| macOS | `文件管家助手`（.command 脚本或 .app） | ≈10MB |

### 7.2 用户界面

```
┌── 文件管家助手 ────────────────── □ ✕ ┐
│                                        │
│  ● 已连接到 192.168.0.101:8080          │
│  设备名：我的台式机                       │
│                                        │
│  正在关注的文件夹：                       │
│    📁 D:\Projects (89 个文件)            │
│    📁 E:\素材库 (38 个文件)              │
│                                        │
│  上次扫描：1 分钟前                      │
│  已备份：3/5 个文件                      │
│                                        │
│  [添加文件夹] [设置]                     │
│                                        │
│  最近活动：                              │
│    1分钟前  发现 2 个新文件               │
│    5分钟前  备份完成：hero_model.c4d     │
│    1小时前  扫描完成：127 个文件          │
│                                        │
└────────────────────────────────────────┘
```

### 7.3 系统托盘（最小化到托盘，不占任务栏）

托盘图标右键菜单：
- 打开文件管家助手
- 立即扫描
- 暂停/恢复
- 退出

---

## 八、服务器端 API（不变，PLAN 已有设计）

Agent 通道 `/api/device/*` 和管理通道 `/api/devices/*` 的 API 设计保持 `PLAN_Phase35_3_device_index.md` 中的方案，因为 UX 优化只改前端和 Agent 外壳，后端数据模型不受影响。

---

## 九、文件夹管理的硬约束

| # | 规则 | 原因 |
|---|------|------|
| 1 | **至少选 1 个文件夹**才能启动管理，空列表不工作 | 用户必须主动划定范围 |
| 2 | 文件夹必须**手动逐一添加**，不自动推荐、不预设 | 隐私 + 用户控制权 |
| 3 | 每台设备独立配置自己的文件夹列表 | 不同设备文件结构不同 |
| 4 | Agent 端校验路径是否**真实存在**，不存在则提示 | 防止配错路径后静默失败 |
| 5 | 移除文件夹 → 对应文件从索引中移除（不删原文件） | 只是不再关注，不动数据 |
| 6 | 移除电脑 → 该设备所有索引/备份任务全部清理 | 干净的脱离 |

---

## 十、开发分期（在 35.3a/b/c 基础上加入 UX 层）

| 期 | 内容 | 验收 |
|----|------|------|
| **35.3a 地基** | 4 表迁移 + Agent 通道 API + 管理通道 API + 配对机制（保持 PLAN 不变） | 回归脚本全链路 |
| **35.3ux 前端+Agent界面** | file_steward_ui.js + file_steward.css + 连接向导 + 文件浏览视图 + Agent GUI/托盘 + 通知接入 + 命名统一 | 3 台设备（本机 + 另一台 Win + 手机浏览器管理端）端到端 |
| **35.3b 备份+自检+提醒** | 分块上传 → L1 内容寻址落盘 + missing/changed 告警 + 备份覆盖率 | critical 资产端到端 |

---

## 十一、一句话总结

> 技术名"设备盘索引" → 用户看到"文件管家"。用户只做三件事：① 指定关注哪些文件夹 ② 看到文件后决定加入哪个项目 ③ 开启自动备份就不用管了。其余全部后台自动完成。
