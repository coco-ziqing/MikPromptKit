# ComfyUI 工作流存储调用空间 — 手动分步骤详细测试流程

- **版本对象**：commit f7e7090 + a125783（2026-08-04）
- **测试方式**：手动 + 浏览器
- **预计耗时**：1 天（可按阶段拆分）
- **测试人**：______　**日期**：______

> 使用说明：每个用例含「操作步骤」与「预期结果」。「操作步骤」按顺序执行；执行后逐条核对「预期结果」，全部符合在「通过」打 ☑，否则在备注记录差异并填写文末缺陷表。

---

## 〇、测试环境与前置准备

### 0.1 确认 PromptKit 服务
**操作**：
1. 打开浏览器（推荐 Chrome/Edge）
2. 地址栏输入 `http://127.0.0.1:8080` 回车
3. 若未登录，输入账号 `admin` / 密码 `admin` 登录

**预期**：页面标题「咪卡词库·个人版」，词库卡片正常显示

### 0.2 确认 ComfyUI 服务
**操作**：
1. 新开标签页输入 `http://127.0.0.1:8189` 回车

**预期**：ComfyUI 界面加载（工作流画布），右上角版本 v0.10.0

### 0.3 确认信号灯
**操作**：
1. 切回 PromptKit 页面，看页面**最底部**的固定信号灯条

**预期**：`● Ollama` 绿灯带延迟 ms、`● ComfyUI` 绿灯带延迟 ms

### 0.4 准备测试素材
**操作**：
1. 在 ComfyUI 中随便跑一张图（任意工作流点「运行」→ 等出图）
2. 在 ComfyUI 出图区右键图片 → 「Save Image」保存到本地，命名 `comfy_test.png`（此图含工作流元数据）
3. 用截图工具截一张屏幕图，命名 `normal.png`（此图不含元数据）
4. 两个文件放到桌面或 Downloads 便于选择

**预期**：两个 PNG 文件就绪

### 0.5 记录 ComfyUI 队列状态
**操作**：
1. ComfyUI 页面右下角/工具栏点「Queue」或观察队列面板

**预期**：记录当前队列状态（应为空）

---

## 阶段 A：环境自检

### A-1 服务监测仪表盘
**操作**：
1. PromptKit 页面顶部导航点「**工具**」下拉
2. 菜单中点击「**服务监测**」
3. 观察弹窗「🖥️ 服务监测仪表盘」

**预期**：
- 弹窗打开，顶部状态徽章显示「已连接 · 时间」
- 内容区显示 CPU 使用率（系统/进程）、内存（总/用/可用）、网络（IP/收发流量）、uptime
- **无**「psutil 不可用」「No module named」类错误

通过 ☐　备注：______

### A-2 完整自检
**操作**：
1. 保持 A-1 弹窗打开，点右上角「**🩺 自检**」按钮
2. 观察自检弹窗逐项运行

**预期**：
- 自检弹窗出现，逐项显示结果（Ollama/ComfyUI/语义/ffmpeg/Pillow/DB/磁盘/端口/LLM）
- ComfyUI 项显示「✓ 正常」及地址 http://127.0.0.1:8189
- 全部完成后可点「关闭」

通过 ☐　备注：______

### A-3 打开工作流库
**操作**：
1. 关闭 A-2 弹窗（点「关闭」）
2. 顶部导航点「**工具**」下拉
3. 点击「**工作流库**」

**预期**：
- 弹窗「工作流库 ComfyUI 存储调用空间」打开
- 顶部绿色状态条显示「● 执行中: 0　◌ 排队中: 0」

通过 ☐　备注：______

### A-4 工作流列表
**操作**：
1. 在 A-3 弹窗中观察「工作流模板」网格区域

**预期**：
- 至少 1 张卡片（「Flux 文生图 (修复版)」或类似名称，来源徽章「Comfy同步/手动」）
- 卡片显示：名称、7 节点、使用次数

通过 ☐　备注：______

### A-5 卡片信息核对
**操作**：
1. 直接观察工作流卡片（无需悬停）
2. 鼠标悬停卡片 1 秒看提示层

**预期**：卡片本体含：封面区（渐变底或图）、右上角来源徽章、下方名称 + 节点数 + 使用次数；悬停显示 tooltip（完整名称/描述/完整提示词）

通过 ☐　备注：______

---

## 阶段 B：工作流库基础功能

### B-1 搜索过滤
**操作**：
1. 工作流库弹窗右上角搜索框（占位符「搜索工作流...」）
2. 输入 `Flux`（不回车，直接输入触发）
3. 观察列表
4. 清空搜索框

**预期**：输入后列表过滤只显示含「Flux」的项；清空后恢复全部

通过 ☐　备注：______

### B-2 选中工作流
**操作**：
1. 点击「Flux 文生图 (修复版)」卡片

**预期**：
- 卡片边框变主题色高亮
- 下方出现「生成」区块，标题显示「『Flux 文生图 (修复版)』· 7 节点」

通过 ☐　备注：______

### B-3 默认提示词
**操作**：
1. 观察生成区「提示词」文本框内容

**预期**：文本框有内容（工作流默认提示词）或为空——记录实际值

通过 ☐　备注：______

### B-4 反复切换无报错
**操作**：
1. 按键盘 `F12` 打开开发者工具，切到「Console」标签
2. 点工作流列表另一张卡片，再点回「Flux 文生图」
3. 观察 Console

**预期**：Console 无红色错误（无 Uncaught / 无 404）

通过 ☐　备注：______

### B-5 空结果提示
**操作**：
1. 搜索框输入 `xyzabc123`（不存在的词）

**预期**：显示「暂无工作流」+ 两条导入指引文案（导入 PNG / 从 ComfyUI 同步）

通过 ☐　备注：______

---

## 阶段 C：三种工作流调用方式

### C-1 在 ComfyUI 中跑图（供捕获）
**操作**：
1. 切到 ComfyUI 标签页（8189）
2. 打开任意可用的工作流（若默认空画布，点菜单 Load 一个示例模板）
3. 点「**Run** / 运行」按钮
4. 等待生成（可在画布右侧看到进度）

**预期**：ComfyUI 队列出现任务并执行

> 不想真实跑图可跳过 C-1、C-2，从 C-3 开始。

通过 ☐　备注：______

### C-2 运行状态栏实时显示
**操作**：
1. 在 C-1 生成期间，切回 PromptKit 工作流库弹窗
2. 看顶部「ComfyUI 运行状态」条
3. 等待 5 秒自动刷新

**预期**：「● 执行中: 1」，右侧详情显示任务 id 前 8 位；任务完成后变为 0

通过 ☐　备注：______

### C-3 从 ComfyUI 同步
**操作**：
1. 等 C-1 任务完成后，工作流库弹窗右上角点「**从 ComfyUI 同步**」
2. 观察 Toast 提示

**预期**：
- Toast「✅ 已同步「从ComfyUI同步」 N 节点」或「已切换」
- 列表新增一条工作流卡片

通过 ☐　备注：______

### C-4 同步条目核对
**操作**：
1. 找到 C-3 新增的卡片，看右上角来源徽章

**预期**：来源徽章为「Comfy同步」，名称「从ComfyUI同步」

通过 ☐　备注：______

### C-5 PNG 导入（含元数据）
**操作**：
1. 工作流库弹窗点「**导入 PNG**」按钮
2. 文件选择框选 `comfy_test.png`
3. 观察 Toast

**预期**：
- Toast「✅ 已导入工作流『……』 7 节点」
- 列表新增卡片，名称取自提示词前 20 字
- 自动选中该新工作流

通过 ☐　备注：______

### C-6 导入后自动填充
**操作**：
1. 观察 C-5 导入后生成区的提示词文本框

**预期**：文本框自动填入从 PNG 提取的提示词（与生成该图的提示词一致）

通过 ☐　备注：______

### C-7 PNG 导入（无元数据）
**操作**：
1. 点「导入 PNG」
2. 选择 `normal.png`（截图）

**预期**：Toast「导入失败: PNG 中未找到 prompt 工作流元数据……」，列表无新增

通过 ☐　备注：______

### C-8 重复导入
**操作**：
1. 再次导入 `comfy_test.png` 两次

**预期**：两次均成功，各生成一条独立工作流（当前不做查重）——记录观察

通过 ☐　备注：______

### C-9 非法文件
**操作**：
1. 新建文本文件改名 `fake.png`（或用任意 .txt 改名）
2. 点「导入 PNG」选择该文件

**预期**：Toast 报错（非有效 PNG 或缺少元数据），页面无崩溃、无白屏

通过 ☐　备注：______

### C-10 词卡右键菜单
**操作**：
1. 关闭工作流库弹窗
2. 在词库卡片网格中找到词卡 **#1482**（名称「a majestic white wol…」之类，今天测试生成的）
3. 在卡片上点鼠标**右键**

**预期**：弹出右键菜单，包含「🎨 AI 生成缩略图」「🧩 用工作流生成」等项

> 找不到 #1482 时：先完成阶段 E 的 E-1 存词卡，再回来测 C-10。

通过 ☐　备注：______

### C-11 词卡调取工作流
**操作**：
1. 在 C-10 的右键菜单点「**🧩 用工作流生成**」
2. 观察页面

**预期**：
- 自动打开「工作流库」弹窗
- Toast「已调取工作流『……』」
- 对应工作流卡片自动选中，生成区出现

通过 ☐　备注：______

### C-12 普通词卡调取
**操作**：
1. 关闭工作流库弹窗
2. 找到普通词卡 **#169**（「微笑」），右键
3. 点「用工作流生成」

**预期**：Toast「词卡未关联工作流……」，不打开工作流库或打开后无选中，页面不报错

通过 ☐　备注：______

### C-13 词卡详情核对
**操作**：
1. 顶部「词库」下拉 → 点「**词卡管理**」
2. 搜索/找到 #1482 词卡，双击或点开详情
3. 查看缩略图与预览图

**预期**：缩略图为 240x160 裁剪图，预览区显示生成的原图 PNG

通过 ☐　备注：______

---

## 阶段 D：参数化前端系统（核心）

### D-1 打开参数配置编辑器
**操作**：
1. 打开「工作流库」弹窗
2. 选中「Flux 文生图 (修复版)」工作流
3. 生成区右上角点紫色「**参数配置**」按钮

**预期**：
- 弹出「参数配置 编辑模式」窗口
- 窗口内列出候选参数，预期 13 项：
  100.ckpt_name（模型）/ 11.text（提示词）/ 12.text（提示词）/ 21.width（宽度）/ 21.height（高度）/ 21.batch_size（批量数）/ 23.seed（随机种子）/ 23.steps（步数）/ 23.cfg（CFG）/ 23.sampler_name（采样器）/ 23.scheduler（调度器）/ 23.denoise（重绘幅度）/ 24.filename_prefix（输出文件名前缀）

通过 ☐　备注：______

### D-2 类型标注核对
**操作**：
1. 在 D-1 编辑器中逐项看「组件」下拉的默认值

**预期**：
- 数字类（宽度/高度/步数/CFG/seed）默认「滑块」
- 文本类（提示词/采样器/调度器/前缀）默认「文本框」
- ckpt_name（.safetensors）默认「select_file」

通过 ☐　备注：______

### D-3 开关编辑器
**操作**：
1. 点编辑器右上角「✕」关闭
2. 再次点「参数配置」打开

**预期**：编辑器正常重新打开，勾选状态为空（未保存不会保留），无报错

通过 ☐　备注：______

### D-4 勾选暴露参数
**操作**：
1. 在编辑器中依次勾选以下 6 项前的复选框：
   - `12.text`（提示词）
   - `23.steps`（步数）
   - `23.cfg`（CFG）
   - `23.seed`（随机种子）
   - `21.width`（宽度）
   - `21.height`（高度）

**预期**：每勾选一项，该项行内展开显示「名称 / 组件 / 范围」配置行

通过 ☐　备注：______

### D-5 自定义命名
**操作**：
1. 找到「23.steps」行的「名称」输入框
2. 把默认「步数」改成「采样步数」

**预期**：名称输入框可编辑，修改生效

通过 ☐　备注：______

### D-6 切换组件类型
**操作**：
1. 找到「23.seed」行的「组件」下拉
2. 从「滑块」改为「数字输入」

**预期**：下拉切换成功（保存后用户模式将显示数字输入框而非滑块）

通过 ☐　备注：______

### D-7 设置范围
**操作**：
1. 「23.steps」行：范围 min 填 `1`，max 填 `60`，步长填 `1`
2. 「23.cfg」行：范围 min 填 `0.5`，max 填 `10`，步长填 `0.1`

**预期**：数字可编辑并保存（生效验证在 D-10）

通过 ☐　备注：______

### D-8 保存并锁定
**操作**：
1. 编辑器顶部「配置名称」输入框填 `基础出图`
2. 点底部「**保存并锁定**」按钮

**预期**：
- 编辑器关闭
- Toast「✅ 已保存并锁定为用户模式」
- 生成区出现「参数模块」徽章：`基础出图 🔒`

通过 ☐　备注：______

### D-9 配置激活
**操作**：
1. 观察「参数模块」徽章与下方表单

**预期**：`基础出图` 徽章高亮（边框主题色），下方渲染参数表单

通过 ☐　备注：______

### D-10 表单控件类型
**操作**：
1. 逐项核对用户模式表单控件：
   - 提示词 → 文本框（2 行高）
   - 采样步数 → **滑块**（可拖动，右侧数值实时变化）
   - CFG → **滑块**
   - 随机种子 → **数字输入框**
   - 宽度 → 滑块
   - 高度 → 滑块

**预期**：控件类型与 D-5/D-6/D-7 设置一致；滑块拖动时右侧数值同步变化；步数滑块范围 1~60，CFG 范围 0.5~10 步长 0.1

通过 ☐　备注：______

### D-11 滑块调整
**操作**：
1. 拖动「采样步数」滑块到 24
2. 观察右侧数值

**预期**：数值显示 `24`

通过 ☐　备注：______

### D-12 修改提示词
**操作**：
1. 提示词文本框清空
2. 输入 `a red panda sitting in a bamboo forest, soft morning light`

**预期**：中文/英文均可输入

通过 ☐　备注：______

### D-13 参数化生成
**操作**：
1. 点生成区「**生成图片**」按钮
2. 观察按钮与状态文字
3. 观察顶部运行状态条

**预期**：
- 按钮变禁用（灰色不可再点）
- 状态显示「⏳ 正在生成（约 15-60s，请勿关闭面板）...」
- 运行状态条「执行中: 1」（5s 内刷新）

通过 ☐　备注：______

### D-14 生成完成
**操作**：
1. 等待生成（耐心等 15-90s）
2. 观察状态与结果区

**预期**：
- 状态变「✅ 生成完成」
- 下方结果区展开，显示生成图片缩略图

通过 ☐　备注：______

### D-15 结果核对
**操作**：
1. 目视检查生成图

**预期**：图中内容与提示词「红熊猫在竹林」匹配；视觉上符合 1024x768（横构图）

通过 ☐　备注：______

### D-16 seed 随机化
**操作**：
1. 不改任何参数，再次点「生成图片」
2. 等完成后对比两张图

**预期**：两张图**不同**（每次 seed 自动随机）

通过 ☐　备注：______

### D-17 下载原图
**操作**：
1. 结果区点「**下载原图**」链接
2. 保存到本地 `result.png`

**预期**：下载成功，文件为 PNG

通过 ☐　备注：______

### D-18 PNG 元数据验证
**操作**：
1. 新建文件 `check_png_meta.py`，粘贴文末附录脚本
2. 命令行运行：`python check_png_meta.py result.png`

**预期**：输出 chunks 包含 `prompt`；「prompt workflow 节点数: 7」，并列出各节点 class_type

通过 ☐　备注：______

### D-19 生成历史
**操作**：
1. 工作流库弹窗底部「生成历史」区，观察表格

**预期**：出现本次记录：时间（今天）、状态 `success`（绿色）、引擎 `comfyui`、耗时约 `xx s`、提示词前 46 字

通过 ☐　备注：______

---

## 阶段 E：生成结果 → 词卡闭环

### E-1 存为词卡
**操作**：
1. 结果区点「**存为词卡**」按钮
2. 观察结果区文字

**预期**：显示「✅ 已存为词卡 #xxx（缩略图已生成……）」，Toast 或结果区提示

通过 ☐　备注：______

### E-2 词库找到新词卡
**操作**：
1. 关闭工作流库弹窗
2. 词库页刷新（F5 或点词库）
3. 找到刚生成的词卡（列表最前或搜索提示词关键字）

**预期**：新词卡显示红熊猫缩略图

通过 ☐　备注：______

### E-3 词卡详情
**操作**：
1. 点开新词卡详情/编辑弹窗

**预期**：预览图 = 生成原图 PNG；内容字段 = 提示词全文

通过 ☐　备注：______

### E-4 闭环复用
**操作**：
1. 对新词卡右键 → 「用工作流生成」

**预期**：工作流库打开并选中对应工作流；Toast「已调取」（若结构与库中已存在工作流相同则复用，不新建重复条目）

通过 ☐　备注：______

### E-5 存为模板
**操作**：
1. 工作流库生成区点「**存为模板**」按钮

**预期**：Toast「已存为模板: ……」，列表新增一条来源徽章「生成」的工作流

通过 ☐　备注：______

### E-6 历史关联词卡
**操作**：
1. 看生成历史最新一条的「词卡」列

**预期**：显示关联词卡号 `#xxx`（不再是 `-`）

通过 ☐　备注：______

---

## 阶段 F：LLM 渐变降级链路

### F-1 批量 AI 缩略图（LLM 方案）
**操作**：
1. 词库页顶部「词库」下拉 → 点「**词卡编辑**」（开启编辑模式）
2. 在 3 张词卡（如 #169 微笑 / #397 / #697）卡片左上角勾选框打勾
3. 底部出现批量工具栏「batchBar」，点「**AI 批量生成缩略图**」
4. 等待完成（每张约 1-3 秒）

**预期**：
- 确认弹窗「确认对 3 条词条批量生成缩略图？」（注意：此弹窗文案提到 ComfyUI，实为旧文案——若出现请记录，见备注）
- Toast「AI缩略图生成: 3/3 成功」
- 顶部 AI 工具条「缩图」按钮也可用（若已勾选则走批量）

> 说明：F-1 用的是「AI 工具条缩图」的 LLM 方案（自动 3 并发渐变生成）；若弹窗要求先配置 ComfyUI 说明走到了旧批量链路，见 F-4 说明。

通过 ☐　备注：______

### F-2 缩略图显示
**操作**：
1. 刷新词库页面（F5）

**预期**：3 张词卡缩略图变为渐变色 ai_thumb_*.jpg（带模块配色与文字）

通过 ☐　备注：______

### F-3 单张 AI 缩略图
**操作**：
1. 任选一张词卡右键 → 「🎨 AI 生成缩略图」

**预期**：Toast「AI缩略图已生成」，卡片缩略图更新

通过 ☐　备注：______

### F-4 降级行为说明（可选）
**操作**：
1. （可选）工具 → 扩展工具 → 取消勾选「启用 ComfyUI 集成」→ 保存
2. 对词卡批量勾选 → 批量工具栏「AI 批量生成缩略图」

**预期**：提示「ComfyUI 未启用，请先配置」并打开配置弹窗（此为旧链路行为，说明批量缩略图仍依赖 ComfyUI；LLM 方案仅通过 AI 工具条「缩图」与右键菜单进入）
3. 测试后：重新勾选启用 ComfyUI 集成并保存

通过 ☐　备注：______

---

## 阶段 G：异常与边界测试

### G-1 未选工作流生成
**操作**：
1. 打开工作流库，**不点选任何卡片**
2. 直接点「生成图片」

**预期**：Toast「请先选择一个工作流模板」，无其他动作

通过 ☐　备注：______

### G-2 空参数保存
**操作**：
1. 选中工作流 → 「参数配置」
2. 不勾选任何参数，直接「保存并锁定」

**预期**：Toast「请至少勾选一个参数」，编辑器不关闭

通过 ☐　备注：______

### G-3 删除参数配置
**操作**：
1. 激活「基础出图」配置后，点徽章上的「✕」
2. 确认弹窗点「确定」

**预期**：配置删除，表单回到简单提示词文本框

通过 ☐　备注：______

### G-4 重复点击生成
**操作**：
1. 点「生成图片」后，在生成期间再连点 3 次

**预期**：按钮已禁用，无重复提交（Toast 只出现一次生成中提示）

通过 ☐　备注：______

### G-5 关闭重开
**操作**：
1. 生成期间点工作流库「✕」关闭
2. 重新打开工作流库
3. 稍后查看生成历史

**预期**：面板正常重开；生成任务在后端继续完成，历史中可见 success 记录

通过 ☐　备注：______

### G-6 停止 ComfyUI
**操作**：
1. 任务管理器（Ctrl+Shift+Esc）找到 ComfyUI 的 python 进程（命令行含 `main.py --port 8189`）
2. 结束该进程
3. 切回工作流库，等待 5-10 秒

**预期**：运行状态条显示「⚠ 连接失败」或「执行中 0」，无页面报错

通过 ☐　备注：______

### G-7 ComfyUI 离线生成
**操作**：
1. 保持 ComfyUI 停止状态
2. 选中工作流 → 点「生成图片」

**预期**：
- Toast「生成失败: 连接被拒」或状态「❌ 无法连接到远程服务器」
- 页面不崩溃
- 生成历史新增一条 `failed` 记录（引擎 comfyui）

通过 ☐　备注：______

### G-8 恢复
**操作**：
1. 重新启动 ComfyUI：运行
   `E:\AI\comfyUI\ComfyUI-BSAI_v010\python\python.exe E:\AI\comfyUI\ComfyUI-BSAI_v010\ComfyUI\main.py --port 8189 --auto-launch --preview-method auto --use-sage-attention --disable-cuda-malloc`
2. 等 8189 可访问（约 1-2 分钟）
3. 回 PromptKit 点「生成图片」

**预期**：生成恢复正常；运行状态条恢复「执行中 0 / 排队中 0」

通过 ☐　备注：______

### G-9 快速连点同步
**操作**：
1. 快速连点「从 ComfyUI 同步」两次

**预期**：两次都有响应（Toast 各出现一次），无堆叠异常/无重复崩溃

通过 ☐　备注：______

### G-10 多弹窗共存
**操作**：
1. 打开工作流库
2. 再打开服务监测（工具 → 服务监测）
3. 再打开扩展工具配置（工具 → 扩展工具）
4. 逐个关闭并切换

**预期**：弹窗层叠正常（工作流库在上层），互不遮挡操作，关闭顺序任意无残留

通过 ☐　备注：______

---

## 阶段 H：回归测试

### H-1 服务监测入口
**操作**：
1. 工具 → 服务监测

**预期**：菜单项存在，仪表盘正常打开（今天修复的入口不回归）

通过 ☐　备注：______

### H-2 批量导出
**操作**：
1. 编辑模式下勾选 2 张词卡
2. 批量工具栏点「TXT」再点「MD」

**预期**：导出文件含词卡内容（不回归双表导出修复）

通过 ☐　备注：______

### H-3 右键菜单完整性
**操作**：
1. 任一词卡右键

**预期**：菜单含：AI 润色增强 / AI 精简压缩 / 格式适配 / 翻译 / AI 分析标签 / AI 生成缩略图 / **用工作流生成** / 复制提示词

通过 ☐　备注：______

### H-4 主流程冒烟
**操作**：
1. 新建词卡：顶部「+」或编辑模式「新建词条」，输入内容保存
2. 顶部搜索框搜该词卡关键字

**预期**：新建成功、可搜索到

通过 ☐　备注：______

### H-5 强刷全量检查
**操作**：
1. 按 `Ctrl + F5` 强制刷新页面
2. F12 Console 观察
3. 依次打开：工作流库 / 服务监测 / 扩展工具 / 词卡管理

**预期**：无 404（除 favicon 外）、无 JS 红错；所有面板可正常打开

通过 ☐　备注：______

---

## 测试总结

| 阶段 | 用例数 | 通过 | 失败 | 阻塞项 |
|------|--------|------|------|--------|
| A 环境自检 | 5 | | | |
| B 工作流库基础 | 5 | | | |
| C 三种调用方式 | 13 | | | |
| D 参数化前端系统 | 19 | | | |
| E 词卡闭环 | 6 | | | |
| F LLM 降级链路 | 4 | | | |
| G 异常与边界 | 10 | | | |
| H 回归 | 5 | | | |
| **合计** | **67** | | | |

### 缺陷记录

| 编号 | 严重度(P0/P1/P2) | 所属用例 | 缺陷描述 | 复现步骤 | 期望 | 实际 |
|------|------------------|----------|----------|----------|------|------|
| | | | | | | |

### 测试结论

- 整体结论：通过 ☐ / 有条件通过 ☐ / 不通过 ☐
- 主要问题：______
- 优化建议：______

---

## 附录：PNG 元数据验证脚本

新建 `check_png_meta.py`：

```python
import sys, json
from PIL import Image

path = sys.argv[1] if len(sys.argv) > 1 else "result.png"
img = Image.open(path)
info = img.info or {}
print("chunks:", list(info.keys()))
if "prompt" in info:
    p = json.loads(info["prompt"])
    print("prompt workflow 节点数:", len(p))
    for nid, node in p.items():
        print(" ", nid, node.get("class_type"))
```

运行：`python check_png_meta.py result.png`

---

*测试环境：Windows 11 / PromptKit :8080 / ComfyUI :8189 (v0.10.0, Flux.1 dev fp8) / Ollama :11434*
