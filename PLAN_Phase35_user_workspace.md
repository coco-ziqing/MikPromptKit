# PromptKit 下一阶段设计方案 — 个人用户「独立工作空间 + 项目文件夹管理」

> 状态：**待用户确认**（设计讨论稿，非开发指令）
> 生成时间：2026-07-14
> 需求来源：多端用户在本地磁盘自定义工作空间 + 项目媒体资产工程文件管理 + 本地文件夹预设 + 每用户独立项目管理

---

## 0. 一句话结论（先看这里）
你想要的「让每个用户在本地磁盘上有自己的工作空间/项目文件夹预设/独立项目管理」，在**当前「单台 Windows 后端 + 局域网浏览器多端」架构**下，有一个**必须先拍板的岔路口**：所谓「本地磁盘」到底指哪台机器的磁盘？不同答案对应完全不同的技术路线。**推荐主线＝服务器端多用户工作空间（解读A）**，其余作可选增强。请先看第 2 节的三选一。

---

## 1. 需求结构化（把口语需求拆成标准需求点）

| # | 需求点 | 标准化描述 | 归类 |
|---|--------|-----------|------|
| R1 | 自定义工作空间 | 用户可创建/命名/选择属于自己的工作空间（Workspace） | 核心 |
| R2 | 本地磁盘绑定 | 工作空间可绑定一个磁盘目录作为「存储根」 | 核心·有约束 |
| R3 | 项目文件夹预设 | 可定义/选择「文件夹结构模板」（如 剧本/角色/场景/分镜/素材/输出/归档），建项目时自动生成目录树 | 核心 |
| R4 | 独立项目管理 | 每个用户能建立、管理只属于自己的项目（私有），与他人隔离 | 核心 |
| R5 | 项目媒体资产工程文件管理 | 项目下的图片/视频/音频/工程文件按目录组织、可浏览/上传/归类/检索 | 核心 |
| R6 | 多端可用 | 手机/平板/另一台电脑通过浏览器都能用上述能力 | 约束 |
| R7 | 可选共享 | 用户可选择把某工作空间/项目设为私有或共享给团队 | 增强 |

---

## 2. 关键架构约束 —— 必须先确认的岔路口 ⚠️

PromptKit 现状 = **1 台 Windows 主机跑后端（FastAPI，data/ 固定目录存文件）+ 其他设备浏览器经 `http://192.168.x.x:8080` 访问**。
浏览器有沙箱：**远程设备的网页无法自由读写「该设备自己的本地磁盘」，也无法读写服务器磁盘（只能走后端 API）**。

因此「用户在本地磁盘制定工作空间」有三种解读，请三选一：

### 解读 A：服务器端多用户工作空间（✅ 推荐主线）
- 「本地磁盘」= **运行后端的那台 Windows 主机的磁盘**。
- 每个用户在服务器磁盘上有独立工作空间根目录（可自定义路径 + 选文件夹预设），远程设备通过 Web UI 管理，文件经**上传**或**服务器端「索引现有文件夹」（Billfish 式）**进入。
- 优点：跨设备一致、稳定、契合现架构、数据集中好备份。
- 代价：文件物理存在服务器主机上，不是各自设备上。

### 解读 B：客户端各自设备本地磁盘绑定（⚠️ 强约束，只能做可选增强）
- 「本地磁盘」= **每个用户各自设备的磁盘**。
- 唯一技术手段＝浏览器 **File System Access API（`showDirectoryPicker`）**，但硬限制：
  1. 需**安全上下文**（HTTPS 或 localhost）——当前局域网是明文 `http`，**远程设备直接不可用**；
  2. **仅 Chromium 系**（Chrome/Edge），Firefox/Safari 不支持；
  3. 目录句柄持久化弱、每次可能要重新授权。
- 结论：只能作为「主机本机浏览器」或「你愿意为局域网启用 HTTPS 自签证书」后的**可选增强**，**不能当主线**。

### 解读 C：每个用户各跑一份 PromptKit 实例（❌ 不建议）
- 每人在自己电脑装一份，「本地磁盘」就是自己的盘。
- 与当前「单主机 LAN 共享 + 多账户在线状态 + 团队协作」定位冲突，且失去协作意义。

> **需要你回答的第 1 题在第 9 节。**

---

## 3. 现状盘点（代码级事实，决定改造量）

| 事实 | 证据 | 影响 |
|------|------|------|
| 所有文件存服务器**固定目录** `data/{thumbnails,originals,videos,project_assets,character_images,...}` | `paths.py` `get_data_dir()`；`media.py` `THUMB_DIR/ORIGINAL_DIR/VIDEO_DIR` | 无「存储根/工作空间根」概念，需新增 |
| **无** workspace/folder/path-preset 相关表或代码 | 全库表名扫描 + 关键字 grep 均无 | 全新模块 |
| `master_project`（主项目实体，2 行）**无 `owner_user_id`** | `PRAGMA table_info(master_project)` | 主项目当前是**全局共享**，无法「私有独立」，需加归属+可见性 |
| `user_project`(seedance)、`media_assets`(473行) **有** `owner_user_id` | Phase18 迁移 ALTER | 部分表已预埋归属 |
| Phase18 给 word_card/collections/wordpacks/library_assets/character_profiles/scene_profiles/media_assets 等**预埋 owner_user_id**，但**值多为 NULL=全局共享，API 未强制按 owner 过滤** | `db_migrate_phase18.py` L405-416；Phase18 决策「旧数据 user_id=NULL→全局共享」 | 「多用户隔离」是**埋了坑没启用**，本阶段要真正激活 |
| `users.settings_json` 存在 | `PRAGMA table_info(users)` | 可存每用户「当前工作空间/偏好」 |
| 已有团队协作：`project_members / squad / 看板 / 里程碑`（按 master_project_id） | Phase22-30 | 共享/协作侧已成熟，私有侧缺失 |

---

## 4. 竞品对比（借鉴点）

| 产品 | 模式 | 对本项目的借鉴 |
|------|------|----------------|
| **Billfish**（免费·本地） | **索引现有文件夹结构**，不搬原文件，DB 单独存隐藏目录；可索引任意路径 | ⭐ 解读A 的「索引现有文件夹」范式；工作空间=映射一个真实目录树 |
| **Eagle**（买断·本地） | 素材**导入进自有库**独占管理，改原路径即失效；强标签/多文件夹归属/插件 | 「托管库」范式（上传即归后端管），标签跨文件夹归类 |
| **MuseDAM / Pixcall**（云/本地混合） | 素材库 + 分类子库 + 元数据筛选 + AI 检索 | 子库层级、元数据检索 |
| **巴别鸟素材库**（企业云） | **子库 + 分类管理员 + 可见性/审批 + 本地映射盘同步 + 操作日志 + 批量重命名** | ⭐ 多用户权限/可见性模型、映射盘同步、审计日志 |
| **Playbook / Brandfolder / Uplifted**（企业创意 DAM） | 集中 hub + 版本控制 + 审批流 + AI 打标 | 版本控制、审批流（本项目已部分具备） |

**提炼给本项目的核心范式**：
1. 工作空间 = 「绑定一个真实目录 + 文件夹结构模板」（Billfish 索引 + 模板生成，二选一或并存）。
2. 私有/共享/团队三级可见性（巴别鸟权限模型的轻量版）。
3. 「不搬动用户原文件」优先（索引在位），降低风险、契合「数据本地不上云」的 SOUL 价值观。

---

## 5. 目标架构方案（基于解读 A 主线）

### 5.1 数据模型（新增/改造）
```
user_workspace (新增)                        -- 每用户可多个工作空间
  id, owner_user_id, name, storage_root,     -- storage_root=服务器磁盘绝对路径
  preset_id, visibility('private'|'team'|'shared'),
  is_default, created_at, updated_at

folder_preset (新增)                          -- 文件夹结构模板
  id, owner_user_id(NULL=系统内置), name,
  structure_json,                            -- 目录树定义，如 ["01_剧本","02_角色",...]
  is_system, created_at

master_project (改造)                         -- 加归属与工作空间绑定
  + owner_user_id INTEGER
  + workspace_id  INTEGER
  + project_root  TEXT                        -- 该项目在磁盘上的根目录
  + visibility    TEXT DEFAULT 'private'

master_asset / media_assets (改造)
  + workspace_id  INTEGER
  + rel_path      TEXT                         -- 相对 project_root 的路径（在位索引用）
```

### 5.2 后端 API（新增 `api/workspace_fs.py`，安全优先）
- `GET  /api/workspaces` / `POST` / `PUT` / `DELETE`：工作空间 CRUD（按登录用户过滤）。
- `GET  /api/folder-presets` / `POST`：预设模板 CRUD（系统内置 + 用户自定义）。
- `POST /api/workspaces/{id}/materialize`：按预设在 storage_root 下生成目录树。
- `GET  /api/fs/browse?path=`：**服务器端安全文件夹浏览器**——限定在白名单根内、**严格防目录穿越（`..`）**、只返回目录/受支持媒体类型。用于「选择本地项目文件夹」。
- `POST /api/workspaces/{id}/index`：Billfish 式**索引现有文件夹**，登记到 media_assets（不搬文件，存 rel_path）。
- 项目相关：`master` 端点全部加 `owner_user_id`/`workspace_id`/`visibility` 过滤。
- 静态服务：新增按 workspace 白名单挂载的媒体读取路由（防越权读盘）。

### 5.3 前端
- 新增「工作空间」入口（header 或用户菜单）：工作空间切换器 + 新建（选路径→选预设→生成）。
- 文件夹浏览器弹窗（走 `/api/fs/browse`）。
- 项目列表加「我的私有 / 团队 / 共享」筛选。
- 项目资产视图：目录树 + 网格预览 + 上传/索引入口。

### 5.4 存储与迁移
- 现有 `data/` 全量归入一个内置「默认公共工作空间」（visibility=shared），零数据丢失。
- 新工作空间默认根：`data/workspaces/<user>/<workspace>/`，也允许用户指定主机上任意可写目录（经白名单校验）。

---

## 6. 模块分层拆解 + 依赖 + 优先级

| 模块 | 层 | 依赖 | 优先级 |
|------|----|----|--------|
| M1 工作空间数据模型 + 迁移（幂等） | 存储 | — | **P0** |
| M2 工作空间/预设 CRUD API | 后端 | M1 | **P0** |
| M3 安全文件夹浏览器 API（防穿越） | 后端 | — | **P0**（安全关键） |
| M4 预设目录树生成（materialize） | 后端 | M1,M3 | P1 |
| M5 master_project 归属+可见性改造 + API 过滤 | 后端 | M1 | **P0** |
| M6 前端：工作空间切换器 + 新建向导 | 前端 | M2,M3 | P1 |
| M7 前端：项目私有/共享筛选 + 资产目录视图 | 前端 | M5 | P1 |
| M8 Billfish 式在位索引 | 后端+前端 | M3 | P2 |
| M9 可见性/权限细化（team 分享） | 后端+前端 | M5 | P2 |
| M10（可选增强）客户端 File System Access（需 HTTPS） | 前端 | 环境 | P3 |

依赖主链：**M1 → M2/M5 → M3 → M4/M6 → M7 → M8/M9**。

---

## 7. 分版本迭代规划（建议 Phase35.x）

- **Phase35.0（P0，地基）**：M1 迁移 + M2 工作空间/预设 API + M3 安全文件夹浏览器 + M5 项目归属&可见性&API 过滤。目标：后端具备「多用户独立工作空间 + 项目私有隔离」能力，附回归测试。
- **Phase35.1（P1，可用）**：M4 目录树生成 + M6 前端新建向导/切换器 + M7 项目筛选&资产目录视图。目标：用户可在 UI 全流程建工作空间、建私有项目、看/传资产。
- **Phase35.2（P2，进阶）**：M8 在位索引 + M9 team 共享权限。目标：对齐 Billfish/巴别鸟核心体验。
- **Phase35.3（P3，可选）**：M10 客户端本地磁盘绑定（前提你愿为 LAN 启 HTTPS）。

---

## 8. 功能边界 / 防过度开发 / 风险

**明确不做（守住 SOUL：纯局域网、不上云、Windows）**：
- ❌ 公网穿透 / 云存储 / 端口映射（AGENTS.md 硬禁止）。
- ❌ 跨设备实时文件同步守护进程（复杂度爆炸，非核心）。
- ❌ 任意挂载远程设备磁盘（浏览器做不到，勿承诺）。

**风险点**：
- 🔴 **目录穿越/越权读盘**：文件夹浏览器与媒体读取必须白名单 + 规范化路径 + 拒绝 `..`；这是本阶段最高安全优先级。
- 🟠 **多用户隔离激活的回归风险**：master_project 从「全局」转「私有」，现有 2 个项目需平滑归入默认公共工作空间，避免老用户「项目消失」。
- 🟠 **打包(EXE)兼容**：storage_root 绝对路径在打包环境下的可写性 + `get_base_dir()` frozen 分支。

---

## 9. 待你确认的关键问题（拍板后即可开工）

1. **【最关键】「本地磁盘」指哪台机器？** 选 A（服务器主机磁盘·推荐）/ B（各自设备磁盘·需 HTTPS 且仅 Chrome）/ C（每人各跑一份）？
2. **工作空间数量**：每用户单一工作空间，还是允许多个？
3. **文件进入方式**：以「上传到服务器托管」为主，还是「索引服务器上已有文件夹（Billfish 式，不搬文件）」为主，还是两者都要？
4. **旧的 2 个 master_project + 全局共享数据**：归入「默认公共工作空间（大家可见）」是否可接受？
5. **可见性**：本阶段是否需要「共享给指定团队/成员」，还是先只做「私有 / 公共」两级？
6. **预设模板**：你心里的标准项目文件夹结构是什么？（给一版你的目录树，我做成系统内置预设）
7. **优先级**：先要「后端能力跑通（Phase35.0）」还是「先出可点的 UI」？

---

## 10. ✅ 已确认决策（2026-07-14）

| # | 结论 |
|---|------|
| Q1 | **混合模式**：服务器主盘只存**关键信息**（索引/指纹/元数据，用于查重·检测·恢复·备份）+ **共享空间**；每人各自**设备盘**存实际媒体资产（图/视频/音频）与工具工程源文件（C4D/AE/PS）。本地+服务器结合。 |
| Q2 | 每人一个工作空间内可**新建并分别管理多个项目空间**，按项目划分。 |
| Q3 | 主盘=关键信息索引（查重/检测/恢复/备份）；设备盘=项目制作媒体与工程文件。两者结合。 |
| Q4 | 通过：旧数据归入「默认公共工作空间」。 |
| Q5 | 通过：先「私有/公共」两级。 |
| Q6 | 通过：先用推荐预设，后续完善模板。 |
| Q7 | 通过：先后端跑通 + 最小验证 UI。 |

## 11. 混合架构 = 「中心轻索引 + 设备本地重资产」

- **服务器 catalog（SQLite）**：每个资产存 指纹(hash) / 文件名 / 大小 / 类型 / 归属设备 / 本地相对路径 / 项目映射 / 小缩略图。→ 支撑查重、缺失检测、恢复清单、备份。
- **设备盘**：原始媒体 + 工程源文件物理留在各自机器。
- **小缩略图上传服务器**：任意设备可浏览预览；全分辨率/工程文件仅在拥有它的设备（或该设备在线时）可打开。

### ⚠️ 遗留的唯一机制决策：服务器如何索引「各自设备盘」的文件？
三选一（仅 Phase35.2 需要，**不阻塞 35.0/35.1 开工**）：
- **M-Agent（推荐）**：每台 Windows 工作站跑一个轻量本地索引代理，后台监视项目文件夹→算指纹→推送 catalog→该设备在线时可经 LAN 回传文件。跨浏览器、可后台、契合「Windows 本地不上云」。代价：每设备装一个小助手。
- **M-FSA**：浏览器 File System Access API 选目录、本地算指纹、只上传元数据。零安装，但**必须为局域网启 HTTPS 自签证书 + 仅 Chrome/Edge + 标签页需开着**。
- **M-Manual**：手动把文件拖进网页仅用于登记元数据。最简单，但无法持续跟踪磁盘目录。

> 手机/平板不涉及工程文件，此机制**只对 Windows 工作站有意义**。

## 12. 修订后分期
- **Phase35.0（可立即开工，无需上述机制）**：工作空间/多项目/预设 + catalog 表(指纹/登记) + 私有隔离 + **共享空间(服务器盘)** + 安全文件夹浏览器(服务器盘)。覆盖 Q1 的「服务器主盘 + 共享」半边 + 私有多项目。
- **Phase35.1**：完整 UI（工作空间/项目/资产目录视图）。
- **Phase35.2**：设备盘索引（落地 M-Agent / M-FSA，视你选择）+ 查重/缺失检测/恢复清单。
- **Phase35.3**：team 定向共享 + 备份策略。
