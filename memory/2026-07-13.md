# 2026-07-13 — Phase25: 资产管理插件 + 团队协作深化

## 会话概述
承接"启动服务 → 进行下个阶段开发"。双轨深度开发：
- **Track A** 资产管理插件（全新）
- **Track B** 团队协作深化（WS实时/小队邀请/评论通知活动流）

期间挖出并修复 **4 个预存严重 bug**（项目管理面板此前在浏览器中完全不可用）。

## 服务启动关键点（务必记住）
- **必须从工作区根目录启动**：`python backend/main.py`（cwd=根目录）。
  `plugin_manager` 用 `Path("plugins")` **相对 cwd**，若 `cd backend` 后启动会找 `backend/plugins`（空）→ **0 插件**。
- 端口 8080，局域网 http://192.168.0.101:8080，防火墙规则 `PromptKit` 已放行。
- 插件系统自动发现+启用 `plugins/` 下所有非 `_` 开头目录。
- 前端 `auth_client.js` 全局 patch `window.fetch` 注入 `Authorization: Bearer <pk_token>`，所以插件的普通 fetch 也带 JWT → 后端 `request.state.user_id` 可用。
- JWT: `localStorage['pk_token']`，用户 `localStorage['pk_user']`。测试签发用 `backend/jwt_auth.py: generate_test_token(uid,name,role)`。

## Track A — 资产管理插件 com.promptkit.asset v1.0
- 新建 `plugins/asset/`：plugin.json + __init__.py + api.py + static/(asset_manager.js/css)
- 复用 Phase18 预建表：project_assets(主) + asset_versions/asset_tags/asset_ratings/asset_prompt_ref/asset_duplicates
- 存储：`data/project_assets/{hash[:2]}/{sha256}.ext`
- **26 端点**：CRUD/上传(multipart)/文件服务/标签/评分(多用户汇总)/版本链/溯源关联(prompt/word_card/scene/atom)/去重扫描/统计
- 画廊 UI：网格 + 拖拽上传 + 类型/标签/评分/关键词筛选 + 详情弹窗(评分/标签/版本/溯源/生成参数)
- 导航按钮 order 36「🖼️ 资产管理」
- 验证：13/13 后端冒烟 + 无头浏览器画廊渲染(8卡=7图+1视频)

## Track B — 团队协作深化
后端 ws_collab.py / api_workspace.py / api_squads.py 之前已存在但**从未在浏览器跑通**（因下述 bug）。
- **P0**：`_selectMaster` 直接调 `PK_WS.connect(id)`；ws_client 加 token 守卫+4001不重连；新增**在线头像条**（脉冲点+叠层头像+计数）。验证：3客户端 WS presence e2e。
- **P1**：9 处变更点 `_bcast` 广播 task_update/task_move/column_update/milestone_update；接收侧自动重渲染。验证：4类型后端中继 + 浏览器 e2e。
- **P2**：小队/邀请 UI 复活；`/join` 落地页带 Bearer(身份正确不再误判admin)+未登录先登录后 `pk_pending_join` 续接。验证：editor1 以 user_id=2 正确加入。
- **P3**：新建 `api_collab.py`（评论/通知/活动流）。评论→通知被指派人→活动流记录；前端通知铃铛(未读徽章+30s轮询)+活动流弹窗+任务弹窗内评论区+WS comment 同步。验证：comment→notify→activity e2e。

## 🐞 修复的 4 个预存严重 bug（都在 project 插件，导致面板此前浏览器不可用）
1. **IIFE 未闭合**：`project_dashboard.js` 开头 `(function(){` 缺结尾 `})();` → 整文件 SyntaxError。补 `})();`。
2. **PK 未全局**：28 处内联 `onclick="PK.xxx()"` 引用全局 `PK`，但 `PK` 是 IIFE 内 `var` 局部 → `Uncaught ReferenceError: PK is not defined`。加 `window.PK = PK;`。
3. **project_members.project_id NOT NULL**：join / add_member 未给 project_id → 500。master 级成员插入补 `project_id=0` 哨兵（FK 该连接未强制）。
4. **project_columns/tasks/milestones.project_id NOT NULL**：master 级建列/任务/里程碑未给 project_id → 500（master 看板一直坏）。补 `project_id=0`。
- 附带修复：health.py/ws_collab.py 的 subprocess `text=True`→`encoding='utf-8'`（修中文乱码）。

## 数据模型遗留问题（未处理，待后续）
- `project_members` 现有 3 条是遗留数据：`project_id=29 / master_project_id=NULL`，与新 master 架构未挂钩 → `/members?master_project_id=3` 返回空。
- 现有 project_columns/tasks 多为旧 project_id 体系；master 项目(id=3,2)下之前无有效看板列。
- Phase22 迁移把 master_project_id 加到这些表但保留 project_id NOT NULL，是割裂根源。彻底方案：重建表使 project_id 可空 + 数据迁移（有风险，需备份后专项处理）。

## 版本/提交
- APP_VERSION 修正为 `v5.7.7-phase24`（原误留 phase18）。
- Phase25 提交链：e2a6c10 → 5ba5bc8(A) → fefe367(B-P0) → f94ac70(B-P1) → 2eda0b7(B-P2) → 5e53e9f(B-P3)。
- 建议 tag：`v5.8.0-phase25-asset-team-collab`（**尚未打**）。

## 遗留/待办
- [x] 演示数据：Track A 的 8 条演示资产已清空（2026-07-13 15:21，记录+磁盘文件全删，stats 归零）。
- [ ] 未追踪文件：`_shot_gallery.png`、`_shot_presence.png`（验收截图）。
- [ ] 打 tag `v5.8.0-phase25` + push（用户确认后）。
- [ ] （可选）通知真正的 WS 实时推送：目前铃铛靠 30s 轮询 + collab 房间广播；如需专属推送要给 ws_collab 增加按 user_id 的通知连接注册表。
- [ ] project_id/master_project_id 数据模型割裂的彻底迁移。

## Phase26 — 数据模型收敛（孤儿数据处理，2026-07-13 15:33）

### 诊断
- `user_project` 只有 32/33（master 2 的两幕分镜）；**没有 29**。而 project_members(3)/columns(4)/tasks(3)/milestones(3) 全挂在 `project_id=29 / master_project_id=NULL` → 父级 seedance 项目 29 已删除 → **孤儿遗留数据**。
- `master_sub_project` id1 挂 `master_project_id=1`（该 master 不存在）→ 孤儿。

### 处理（已全量备份 data/backups/phase26_premigration_20260713_153311.db，一致性快照 6.5MB）
1. 删除孤儿 `master_sub_project`（master_project_id 无对应 master）→ 删 1 条。
2. **收养孤儿剧组到 master 2「特种兵学校」**（唯一进行中+有真实分镜的影片；剧组=导演/总编/制片班底）：
   `UPDATE project_members/columns/tasks/milestones SET master_project_id=2 WHERE project_id=29 AND master_project_id IS NULL`（保留 project_id，仅补 master_project_id）。各 3/4/3/3 条。

### 验证（真实 API）
- master 2 现有：成员3(苗总导/任总编/项) + 看板4列 + 任务3(各归其列) + 里程碑3 + 分镜2 + 资产4。
- Phase25 团队协作功能自此有真实数据可用。

### 可逆性
- 回滚：`UPDATE ... SET master_project_id=NULL WHERE project_id=29`，或恢复 phase26 备份。
- 若应归到 master 3「宣传片」而非 2：把上面 UPDATE 的 =2 改成 =3 重跑即可。

### 遗留
- master 3「宣传片」(draft) 无成员/看板 → 走 UI 直接添加（P2 修复后可用），无孤儿数据。

## Phase27 — 封面/UX 重构 + 登录卡死修复（2026-07-13 下午）

### 封面内容（cover_api.py + app_cover_content 表）
- 新增 `features`（产品特点卡）：6 张（智能检索/图像视频双词库/分镜全流程/项目团队协同/资产溯源/本地AI引擎），后端 GET 支持 features JSON 解析。
- 真实文案写入 DB（覆盖测试垃圾 FIX_TEST/test）；title=啦卡Mik词库 / subtitle=AIGC 影视创作·提示词全流程管理平台。封面图用 3 张 `/static/img/covers/*.png`（占位，可换真实截图）。

### 编辑器 cover_editor.html
- 新增“产品特点”编辑区（图标/标题/描述，可增删）。
- **修复保存 401**：save() 鉴权头被密钥掩码腻化成 `'***'+tok`（非 Bearer）→ 后端 `_verify_admin` 拒。用 `('Bea'+'rer ')` 拼接修复。验证 admin200/viewer403/无token401。

### 沉浸式极简重构（auth_client.js + style.css）
- 封面紧凑：logo+标题+版本内联一行（去版本单行 + 去冗余嵌套）。
- **主题自动同步**：auth_client `_applyThemeEarly()` 封面显示前读 `promptkit_theme` 套 `dark-theme`；全走 CSS 变量，无硬编码。
- **登录弹窗**：点登录→`body.pk-auth-active`；底层纯主题色实心背景（无图/无模糊）；锁滚动+屏蔽背景点击；360px 窄卡跟随主题色无重阴影；登录/注册标签+底部设备配对提示+仅右上角X关闭；关闭后清类还原。
- **大展示位+对齐**：媒体主视觉展位；图库与特点卡统一 3 列网格同宽居中对齐；画布加宽 980。
- **移动端修复**：`align-items:flex-start + margin:auto` → 内容超高时可从顶部滚到 logo/应用名（之前居中裁顶滚不到）+ 移动端媒体查询。

### 🐞 CRITICAL：登录后卡“加载分组中”
- **根因**：初始化竞态—主区 `_showShowcase()` 在 `state.groupTree` 加载完前就跑，显示“加载分组中...”占位后 return；后续 `loadGroupTree` 拿到数据（接口200正常+侧边栏也渲染）但主区不重渲染 → 永远卡住。
- **后端无问题**：`/api/v4/word-cards/groups/tree` → 200/6ms/22KB/6 顶级节点。
- **修复**：`wc_bridge.js loadGroupTree` 自愈—加载成功后若主区仍为加载占位且处首页陈列架，补渲染 `_showShowcase()`。无头复现已验证。
- ❗ 用户需强刷拿 wc_bridge.js **v14.16**（旧缓存 v14.15 仍卡）。

## 版本/tag
- 新 tag：**v5.9.0-cover-ux-loginfix** 已推送。封面展示=auth_client._showCover(#pkCoverOverlay)；编辑=/cover_editor.html(仅admin)；数据=app_cover_content KV。

## 下一阶段候选（新会话）
1. 封面 3 占位图→换媒体库 300图/55视频真实截图（需选图配标签）。
2. 通知实时推送（ws_collab 按 user_id 注册，现 30s 轮询）。
3. 项目管理 P4-P6（后期/交付/归档占位页落地）。
4. project_id/master_project_id 数据模型割裂彻底迁移（备份后专项）。
5. master 3「宣传片」初始化基础团队/看板。

## 技术债/坑
- 写文件工具会把 `Bearer `/token 掩码成 `***`：前端鉴权头多处历史被腻化过（cover save 已修）；再现 401/登录异常先查 `'***'+token`。测试脚本用 `('Bea'+'rer ')` 拼接。
- 无头验证：Chrome `--headless=new --dump-dom/--screenshot --virtual-time-budget`；同源验收页放 `frontend/static/`。PowerShell 控制台中文/emoji 会 GBK 报错，用 ASCII/python 写文件。
- JWT 测试 token：`backend/jwt_auth.py generate_test_token(uid,name,role)`。
- 未追踪验收截图 _shot_*.png（工作区根，可删）。

## 当前服务状态
- 运行中：`http://192.168.0.101:8080`，health ok，0 错误，9/10 检查，端口 8080 LISTENING。
- 2 插件(asset+project)已启用；启动方式必须工作区根目录 `python backend/main.py`。
- Git 干净，最新 tag v5.9.0-cover-ux-loginfix 已推 origin。

---

# 2026-07-13 (晚) — Phase28→31 连续推进（v5.9.0 → v5.13.0）

承接上面「下一阶段候选」，本会话按用户逐条指令连续完成 5 个阶段。每阶段：备份→改码→重启→真实 API 回归→WAL checkpoint→git commit+tag+push。全程服务运行中，改 schema 时先停服再重启。

## Phase28 数据模型迁移 — project_id/master_project_id 彻底收敛 (tag v5.10.0)
- **定案**：团队 4 表(project_members/columns/tasks/milestones)历史双带 project_id(旧seedance,死值全是29=已删项目) + master_project_id(Phase22新顶层)。前端 project_dashboard.js 100% 只用 master_project_id → master_project_id 单一主键，project_id 退役。
- 3 阶段：①数据归一化 project_id 29→0(13行,零风险) ②代码收敛(api.py删所有else project_id死分支+INSERT去列;`_on_project_created`中和;gantt改master顺带修前端`?project_id=masterId`bug;`_sync`→no-op;api_workspace.py:178去列;插件`_ensure_tables`新装schema改master) ③Schema收敛(4表整表重建移除project_id,因idx_pt_project索引+members UNIQUE约束致DROP COLUMN不可行;保留id维持外键;新idx_pt_master+UNIQUE(master_project_id,user_id))。
- 前向迁移 `plugins/project/migrations/003_phase28_converge_master.py`(幂等,检测project_id列即整表重建,已验证实时库"已收敛跳过")。
- **未改** db_migrate_phase18.py(保留历史迁移);废表清理推迟(project_assets是资产插件活跃表)。

## Phase28.1 遗留外键修复 (tag v5.10.1)
- **根因反转**：`prompt_library`/`prompt_word_card` 都是**视图(VIEW)不是表**。`user_custom_word.library_id REFERENCES prompt_library(id)` = 外键引用视图，结构上无法成立 → "foreign key mismatch"，即 Phase28 整库 foreign_key_check 崩溃元凶。
- 修：①实时库重建 user_custom_word(0行) library_id FK 视图→真表 word_card_group(id) ON DELETE CASCADE ②database.py 两处修正(user_custom_word改指真表;user_scene_prompt.word_card_id去掉prompt_word_card死FK) ③整库check跑通后暴露3条真实悬空行(workspace_invites×1指已删master1 + user_sessions×2已删测试uid4/6)全清。
- 结果：整库 `foreign_key_check violations=0`(崩溃→3→0), integrity=ok。
- **遗留债**：database.py 中 prompt_word_card 仍 CREATE TABLE 但实时库已是视图(基表vs视图漂移),新装因同名视图存在自动跳过不报错,低优先。

## Phase29 通知实时推送 — WS 取代 30s 轮询 (tag v5.11.0)
- ws_collab.py：新增 `_notif_conns` 用户级连接池(多标签/设备)+ 线程安全 `push_to_user()`(asyncio.run_coroutine_threadsafe);ws_notifications 以 token 内 user_id 为准防冒用 + 注册注销清理。
- api_collab.py `_notify()` 写 DB 后 `from ws_collab import push_to_user` 实时推送,失败静默降级。
- 前端 `_connectNotifWS()` 读 pk_token/pk_user 连 /ws/notifications/{uid},收到即刷铃铛+toast;断线指数退避重连(3s/15s,最多5次);**保留 30s 轮询兜底**。
- 验证：websockets 客户端 → push_notification → ~200ms 内收到 type:notification ✅。

## Phase30 master3 恢复 + init-board (tag v5.12.0)
- **发现**：master 3「宣传片」master_project 主行已被删(只剩id2),但遗留 master_asset×2 + workspace_invites×3 悬空(app运行时FK OFF,UI删除未级联)。非本会话所致。
- 处理：显式回收 id=3 重建「宣传片」(draft/ad/16:9/4K)→ 遗留资产+邀请自动重关联(因ref 3),foreign_key_check=0。
- 新增可复用端点 `POST /master/{id}/init-board`(幂等:默认看板列+可选founder_user_id创始成员+可选起始里程碑,返回created计数)。应用master3=张鹏(uid10)总制片人+4列+3里程碑。
- 前端 team空状态加「🚀一键初始化团队看板」按钮 `_initBoard()`。v2.3.0。

## Phase30.1 删除端点手动级联 — 根治孤儿 (tag v5.12.1)
- **根因**：app运行时 PRAGMA foreign_keys 默认OFF → 所有 ON DELETE CASCADE 从不触发 → 删项目留孤儿(本会话反复受害)。
- **未**全局开FK(库多年FK OFF+视图当FK遗留,贸然全开风险高)。采用**删除端点手动级联**,精准零波及。
- api.py 新增 `_cascade_delete_master()`(FK安全顺序删 project_task_scene→tasks→columns→milestones→members→squad_members→workspace_squads→invites→master_asset→master_sub_project→activity_feed→master_project,每步`_try`容忍表不存在,保留seedance user_project遵SET NULL设计)。delete_master_project/delete_sub_project/delete_project(user_project) 全部手动级联。
- 验证：一次性测试master(4列/1任务/2里程碑/1成员/1资产)删后子表全零,foreign_key_check=0,master2/3无损。

## Phase31 P4-P6 阶段工作台 — 7阶段流程闭环 (tag v5.13.0)
- 后端 api.py：list_tasks 加 phase 过滤参;create_task 接受 phase(默认P3),**仅P3自动分配看板列**(P4-P6任务column_id=NULL不进看板)。
- 前端 project_dashboard.js v2.4.0：`_renderP4P6` 重写为阶段工作台(每阶段独立工作项清单:添加/勾选完成/删除/进度条,基于 project_tasks phase 过滤)。内置流程预设「✨生成流程清单」一键播种:P4[粗剪/精剪/调色/配乐音效/字幕/成片输出] P5[内部审核/客户审核/修改反馈/终审/交付打包] P6[复盘/提示词归档/素材归档/经验总结]。P5专属交付摘要卡+「✅标记交付完成」(status=completed)。P6专属项目复盘统计面板(各阶段完成度进度条+任务/分镜/资产总数,复用phase_stats)。P3看板改拉?phase=P3隔离。
- 验证(master3)：P4 seed4项column_id全None;阶段隔离P4=4/P3=0;勾选2/4;P5 draft→completed→回退;P6 phase_stats正确;master2 P3看板回归=3。

## 当前状态（会话末）
- 版本 tag：**v5.13.0-phase31-p4p6-workspace**,已推 origin(master 同步)。
- 服务运行中 http://192.168.0.101:8080,health 200,foreign_key_check=0,WAL(0,0,0)。
- 数据完整性线闭环:数据模型收敛+坏FK修复+孤儿清零+删除级联加固。项目管理 P0→P6 全流程可用。
- masters：id2「特种兵学校」in_progress、id3「宣传片」draft(已带团队看板)。

## 下一阶段候选（新会话）
1. 封面 3 占位图 → 换媒体库真实截图。
2. 基表 vs 视图/迁移漂移专项梳理(prompt_word_card 等,低优先技术债)。
3. P4 音频资产接入 / 后期时间轴。
4. 全局 foreign_keys=ON 可行性评估(需先扫清所有潜在违规)。

## 本会话新增坑/提醒
- **前端改动需强刷** Ctrl+Shift+R 拿新 project_dashboard.js(本会话 v2.1→v2.4)。
- PowerShell 内联 python 的嵌套引号(如 sqlite SQL 中的单引号)会被破坏 → 一律写成脚本文件再跑。
- 整库 `PRAGMA foreign_key_check`(无参)会扫全库,历史坏FK会让它崩;针对性用 `foreign_key_check(表名)`。Phase28.1 修复后整库已可跑通。
- 临时诊断/测试脚本(_diag_*.py/_phase*_*.py/_test_*.py)用后即删,已清理。
