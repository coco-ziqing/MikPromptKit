# PromptKit 会话记忆 — 2026-06-02

## 卡片布局重构（2026-06-02）
- 从「水平布局」（左缩略图|右文字|收藏列）改为 **垂直布局**（缩略图置顶 → card-add-row → 文字内容 → 按钮）
- **`.card-body`** `flex-direction: column`，缩略图 100% 宽度
- **`.card-add-row`** 新增容器：在缩略图与文字之间，左放 `+` 按钮，右放 `card-collections`（收藏徽标+复选框）
- **`.card-collections`** 从 `position: absolute` overlay 改为 `margin-left: auto` 靠右对齐
- **`.coll-add-btn`** 从缩略图右下角 overlay 移入 card-add-row 内

## 按钮底部对齐修复
- `.card-content` 新增 `flex-grow: 1` 主动撑满空间
- `.card-actions` 用 `margin-top: auto` 吸底
- `.card-meaning`、`.card-scene` 加 `flex-shrink: 0` 防止压缩
- 所有列（1~6 列）通用

## 缩略图尺寸调整
- `applyColumns()` 缩略图值修正：
  - 1列: 960→480→260→**480×320**（最终）
  - 2列: 340→**190×127**
  - 3列: 140×93（不变）
- CSS 安全栓: `max-width: 85%`

## CSS 冲突修复
- 删除第二套裸类名 `.card-thumb` 块（收藏弹出菜单区，`overflow: visible` + `height: 76px` 覆盖第一套）
- 缩略图子类全部加 `.prompt-card .card-thumb` 前缀防冲突

## 收藏夹显示修复
- 后端 `GET /collections/{cid}/items` 补充 `collections` 字段返回
- `renderCollectionItems()` 新增 `collHtml` 构建和 `isSelected` 定义
- `_doQuickCollect()` 收藏后刷新 `loadCollectionItems()`
- `createCollection()` 新建收藏后刷新收藏夹列表

## 已知问题
- 收藏夹内添加词条后需刷新才能显示收藏徽标（需点击 + 按钮）
- 1列时缩略图较大（480px宽），卡片整体偏大

## 服务状态
- 端口：8080
- 内网IP：192.168.0.103
- 访问地址：http://192.168.0.103:8080
- 服务运行正常
