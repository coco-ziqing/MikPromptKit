# PromptKit — 2026-06-11 会话关闭备忘

## 版本
- Git tag: `v4.0.0-phase8`
- Commit: `1f0b279`
- 基于: `87fc6f4` (景别词库)

## 本次会话涉及的主题

### 组装器 UI 布局优化 (Phase 7 延续)
- ⚠️ 多次尝试移除编辑器顶部 header 导致 JS 括号/结构破坏，反复回退
- ⚠️ 尝试添加文本段落模式 (彩色段落+tooltip入库) 因括号不匹配问题反复失败
- ⚠️ 部分提交被 revert 或 reset 丢弃

### 最终保留的稳定改动 (v4.0.0-phase8)
| 文件 | 改动 |
|------|------|
| `app.js` | URL hash `#/seedance/composer` 自动切到组装器 tab |
| `index.html` | JS 版本号 v=15 (防缓存) |
| `seedance_v2.py` | shot_scale 从 extra_fields 移除 |
| `style.css` | 右侧面板 position:relative |

### 未完成的尝试
| 功能 | 状态 |
|------|------|
| 移除编辑器 header | ⚠️ 恢复保留，移除时破坏 JS 结构 |
| 文本段落彩色模式 | ⚠️ 已 revert，括号错配导致页面不加载 |
| 项目名双击编辑 | ⚠️ 已部分实现但冲突，需重新干净实现 |
| 全屏按钮 | ❌ 未开始 |
| 输出面板移到右侧 | ❌ 未开始 |

### 当前运行状态
- 服务: http://127.0.0.1:8080 / http://192.168.0.103:8080
- 数据库: 213 提示词 / 189 卡片 / 322 词库 / 31 词库维度
- 演示项目: id=73 (4镜头)

### 下次打开建议
1. 读取 HEARTBEAT.md + MEMORY.md 恢复上下文
2. 启动服务: `start.bat` 或 Python 直接启动
3. 从 87fc6f4 (景别词库) 基准开始，**用脚本文件一次性完成所有改动**
4. 优先修复：项目名交互 (侧栏双击编辑+切换保存)
5. CSS/JS 改动用 Python 脚本批量操作，避免手动 edit 工具导致的括号错配
# PromptKit — 2026-06-11 会话关闭备忘

## 版本
- Git tag: `v4.0.0-phase8`
- Commit: `1f0b279`
- 基于: `87fc6f4` (景别词库)

## 本次会话涉及的主题

### 组装器 UI 布局优化 (Phase 7 延续)
- ⚠️ 多次尝试移除编辑器顶部 header 导致 JS 括号/结构破坏，反复回退
- ⚠️ 尝试添加文本段落模式 (彩色段落+tooltip入库) 因括号不匹配问题反复失败
- ⚠️ 部分提交被 revert 或 reset 丢弃

### 最终保留的稳定改动 (v4.0.0-phase8)
| 文件 | 改动 |
|------|------|
| `app.js` | URL hash `#/seedance/composer` 自动切到组装器 tab |
| `index.html` | JS 版本号 v=15 (防缓存) |
| `seedance_v2.py` | shot_scale 从 extra_fields 移除 |
| `style.css` | 右侧面板 position:relative |

### 未完成的尝试
| 功能 | 状态 |
|------|------|
| 移除编辑器 header | ⚠️ 恢复保留，移除时破坏 JS 结构 |
| 文本段落彩色模式 | ⚠️ 已 revert，括号错配导致页面不加载 |
| 项目名双击编辑 | ⚠️ 已部分实现但冲突，需重新干净实现 |
| 全屏按钮 | ❌ 未开始 |
| 输出面板移到右侧 | ❌ 未开始 |

### 当前运行状态
- 服务: http://127.0.0.1:8080 / http://192.168.0.103:8080
- 数据库: 213 提示词 / 189 卡片 / 322 词库 / 31 词库维度
- 演示项目: id=73 (4镜头)

### 下次打开建议
1. 读取 HEARTBEAT.md + MEMORY.md 恢复上下文
2. 启动服务: `start.bat` 或 Python 直接启动
3. 从 87fc6f4 (景别词库) 基准开始，**用脚本文件一次性完成所有改动**
4. 优先修复：项目名交互 (侧栏双击编辑+切换保存)
5. CSS/JS 改动用 Python 脚本批量操作，避免手动 edit 工具导致的括号错配

---

# 第2轮会话 — 2026-06-11 下午 (Phase 7.1 → 7.4-hotfix)

## 版本基线
- 起始: `v4.0.0-phase8` (1f0b279)
- 当前: `v4.0.0-phase7.4-hotfix` (7548be1)

## Phase 7.1 — 字段清理 (f7b02fa)
| 改动 | 文件 |
|------|------|
| shot_scale 从前端 8 处引用全链移除 | seedance_v2_composer.js |
| shot_scale 从后端 updatable 列表移除 | seedance_v2.py |
| 空壳自定义分组 id=29 (cards=0) 删除 | prompts.db |

## Phase 7.2 — 性能+交互优化 (67a7ebe)
| 改动 | 说明 |
|------|------|
| _debouncedCompose() 300ms 防抖 | compose 高频调用点 6 处 →
| preloadAllCardCaches() 后台预加载 | init 时 28 词库异步加载 |
| 导出文件命名规范 | `项目名_20260611_1044_3s_运镜.json` |

## Phase 7.3 — 拓展单元选词即时跳转 (50e589d)
| 操作 | 旧行为 | 新行为 |
|------|--------|--------|
| 下拉切换分组字段 | ❌ 只改字段名 | ✅ 右侧面板自动打开对应词库 |
| 点击 `+` 新增单元 | ❌ 仅加空字段 | ✅ 自动打开词库词卡列表 |

## Phase 7.4 — 词卡缩略图预览系统 (50fa644)
| 层面 | 内容 |
|------|------|
| 后端新端点 | POST /cards/{id}/thumbnail (Pillow 3:2→100×67) + DELETE + GET /thumbnails/file |
| 前端渲染 | 右侧面板卡片: 左 100×67 缩略图 + 右文字 |
| 拖拽上传 | .s2-card-thumb-zone 区域 dragover/drop → FormData POST |
| 目录 | data/wordcard_thumbs/ |

## Phase 7.4-hotfix — JS 运行时崩溃修复 (7548be1)
| 故障 | 根因 |
|------|------|
| 组装器页面全部功能丢失，只显示搜索框 | onerror="this.style.display=\\'none\\'" 转义链断裂 |
| 修复 | 移除 onerror，改用 CSS 自然降级 + 无图时 `<span>+</span>` 占位 |
| 影响范围 | `_renderRightPickerContent` 中词卡渲染行 |

## 已知未修复问题
- `environment_detail` ↔ `env_detail` 维度 key 不一致（低级，不影响功能）
- 项目名双击编辑交互不完善
- 自定义分组管理入口隐藏较深

## 当前服务状态
- 版本: JS v=20
- 端口: 8080 ✅
- 词库: 28 维度 / 371 词卡
- 项目: 5 项目 / 12 镜头
- 缩略图系统: wordcard_thumbs/ 已就绪 (空，待用户上传)
