# 2026-06-24 会话记忆

## 会话概要
- 日期: 2026-06-24 (周三)
- 时间段: 11:00 - 15:00 (Asia/Shanghai)
- 主线: PromptKit v5.0 原子化提示词工业化平台 Phase15-17 开发
- 版本: v4.2.0-phase14-arch → v5.0.0-phase16-atom-editor

---

## 核心成果

### Phase15: 后端原子引擎加固 (已完成)
- **atoms.py**: 5→12 端点 (OCR图片提取/文本拆解/归档分组/列表/桥接/删除/统计)
- **atoms_import.py**: 新建 — CSV/JSON/TXT 三格式批量导入中间件
- **migrate_atom_tables.py**: 新增 atom_word_bridge 表 + 6 个索引
- **关键修复**: qwen3.5 思维模型 token 预算 (600→4000)、JSON 鲁棒提取、英文 system prompt
- **main.py**: 版本号 v5.0.0-phase15-atom-engine, 加载 atoms_import 路由
- **真实 Ollama 验证**: 通过 — 6 原子拆解, 质量分 1.0, MD5 缓存命中 0.01s

### Phase16: 前端原子编辑器 (已完成)
- **atom_editor.js**: 420+ 行 — AI拆解面板/进度条/原子卡片/展开折叠/列表/删除/变异生成/归档
- **style.css**: +40+ 规则 (三栏布局/卡片/进度条/统计行/深色适配)
- **index.html**: 导航按钮「⚛ 原子引擎」+ 脚本加载 + 版本号 5.0.0
- **Git tag**: v5.0.0-phase16-atom-editor

### Phase17: 全链路贯通 (已完成)
- **atoms.py**: 新增 GET /list, GET /decompose/{did}/bridge, DELETE /decompose/{did}
- **atom_editor.js**: 列表分页渲染, 展开折叠, 删除确认, 变异生成弹窗
- 路径修复: 路由顺序 `GET /list` → `GET /decompose/{did}` 避免冲突
- 级联删除: DELETE 同时清理 variation + bridge + stats

### 代码审查 (已完成)
- 范围: 4 核心文件 ~1,800 行
- P0 (2): MAX(id) 竞态 → last_insert_rowid(), XSS innerHTML 转义
- P1 (6): archive-to-group 去重, N+1 commit, 死代码 DECOMPOSE_SYS, 导入串行
- P2 (7): inner import re, alert() → 模态框, CSV 解析脆弱, 颜色对比度

---

## Ollama 故障诊断实录 (重要经验)
1. `qwen3.5:9b` 是思维模型, `num_predict` = thinking + content 共享
2. 600 tokens 全被 thinking 占满 → content=0 → `json.loads("")` 崩溃
3. 修复: max_tokens=4000, 路由 decompose 到 qwen3.5:4b(更快)
4. 英文 system prompt 产生更稳定 JSON 输出
5. fallback: `_extract_json_array()` 鲁棒解析 (去代码块 + 重新定位数组)

---

## P0-P1 问题修复计划 (待执行)
- [ ] P0-1: MAX(id) → last_insert_rowid() (4 处)
- [ ] P0-2: HTML 转义函数 + 6 处调用
- [ ] P1-1: archive-to-group 去重
- [ ] P1-2: N+1 commit 合并
- [ ] P1-3: safe_count_dict 验证
- [ ] P1-5: 删除 DECOMPOSE_SYS 死代码
- [ ] P1-6: 导入并发 asyncio.gather

---

## Git 里程碑
- `8fa345c` — v5.0.0-phase15-atom-engine
- `fbb9ae8` — v5.0.0-phase16-atom-editor
- 当前 HEAD: v5.0.0-phase16-atom-editor

## 数据库状态 (49 表, 5 原子表)
- atom_decompose: 有数据 (真实 Ollama 拆解)
- atom_variation: 有数据
- atom_template: 0 行
- atom_stats: 0 行
- atom_word_bridge: 0 行 (待归档)

## 端口占用状态
- 8080: 旧版服务
- 8081: 旧版服务 (可能残留)
- 8087: Phase17 测试服务 (当前活动)
- 启动时端口自增: 8080→8089 范围探测

---

## Phase17.1 原子分组树形结构修复 (2026-06-24 16:48)

### 问题
全部词组页面中，[原子] 图像词库 / [原子] 视频词库根目录下没有显示子原子分组（如主体描述、光影效果等）。

### 根因
`wc_bridge.js` 的 `_showShowcase()` 渲染逻辑只处理 `root → sub → leaf` 三层结构，原子分组是 `root → atom（叶子）` 两层结构，被这一行跳过：

```js
if (!sub.children || sub.children.length === 0) continue;  // ← atom 分组被跳过
```

### 修复方案
在 `_showShowcase()` 的子循环结束后（`}` 闭合后）插入 atom 叶子渲染代码：

**位置**: `wc_bridge.js` 约 L195，`html += '</div></div>';` 之前（sub 循环 `}` 之后，`html += '</div></div>';` 之前）

**插入代码**:
```js
            // Phase17: atom 原子分组（root 直接叶子，无 sub 中间层）
            var atomLeaves = [];
            for (var a = 0; a < root.children.length; a++) {
                if (root.children[a].group_type === 'atom') atomLeaves.push(root.children[a]);
            }
            if (atomLeaves.length > 0) {
                var atomTotal = atomLeaves.reduce(function(sum,g){return sum+(g.card_count||0);},0);
                html += '<div style="border-left:2px solid var(--border-color);margin-left:8px;margin-bottom:6px;padding:6px 0 6px 12px;border-radius:0 8px 8px 0;">';
                html += '<div style="display:flex;align-items:center;gap:6px;padding:4px 0;font-size:13px;color:var(--text-muted);font-weight:600;">';
                html += '<span style="font-size:15px;">⚛️</span><span>原子分组</span>';
                html += '<span style="font-size:11px;margin-left:auto;">' + atomTotal + ' 条</span></div>';
                html += '<div style="display:flex;flex-wrap:wrap;gap:6px;">';
                for (var g = 0; g < atomLeaves.length; g++) {
                    var grp = atomLeaves[g];
                    html += '<button onclick="event.stopPropagation();App.switchGroup(' + grp.id + ',\'' + (grp.name||'').replace(/'/g,"\\'") + '\')" ';
                    html += 'class="showcase-leaf-btn" style="font-size:13px;padding:6px 14px;border:1px solid var(--border-color);border-radius:8px;background:var(--bg-card);color:var(--text-main);cursor:pointer;white-space:nowrap;transition:all 0.15s;line-height:1.5;"';
                    html += ' onmouseenter="this.style.borderColor=var(--primary);this.style.background=var(--hover-bg)" onmouseleave="this.style.borderColor=var(--border-color);this.style.background=var(--bg-card)"';
                    html += '>';
                    html += (grp.icon||'📄') + ' ' + App._escape(grp.name.replace(grp.icon||'','').trim());
                    html += '<span style="font-size:10px;color:var(--text-muted);margin-left:4px;">' + (grp.card_count||0) + '</span>';
                    html += '</button>';
                }
                html += '</div></div>';
            }
```

**版本号**: `wc_bridge.js` v9.6 → v10

### 待执行
1. 在 `wc_bridge.js` 中手动插入上述代码（第196行前）
2. 重启服务后 Ctrl+Shift+R 刷新页面
3. 验证全部词组页面中 [原子] 图像词库根目录下能看到原子子分组按钮
