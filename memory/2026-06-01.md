# 2026-06-01 会话记忆

## 本次会话完成的工作

### ComfyUI ↔ PromptKit 联动集成（v3.1.3 → v3.2.0）
1. **ComfyUI 缩略图自动生成** — 编辑弹窗 "AI 生成" 按钮发送提示词到 ComfyUI，等待完成后自动下载裁剪为 3:2 缩略图并关联到提示词
2. **工作流一键同步** — `POST /api/v2/comfyui/sync` 从 ComfyUI queue/history 自动提取最新工作流，检测提示词节点和输出节点，导入到配置
3. **模块主体预设** — 每个模块可设置共性主体描述，AI 生成时自动组合 `预设主体 + 卡片内容 + 品质后缀` 作为完整提示词发送给 ComfyUI
4. **批量 AI 生成** — 批量选择模式新增 "✨ AI 批量生成缩略图" 按钮，逐条排队生成
5. **缩略图/视频切换修复** — AI 生成图片后自动清除视频关联（`DELETE FROM prompt_videos`），确保前端显示静态缩略图而非视频预览
6. **自动刷新链路** — 生成完成后编辑预览、卡片网格、图库窗口三处同步刷新
7. **路径修正** — `comfyui.py` 缩略图目录路径从 `backend/data/` 修正到 `data/`
8. **超时修正** — `fetchJSON` 增加 `timeoutMs` 参数，生成调用使用 10 分钟超时

### 修复的 Bug
- 图库不显示新生成图片 → loadThumbLibrary 改为无条件执行
- 生成后手动刷新才显缩略图 → fetchJSON 默认 30s 超时导致 abort
- 模块预设看不到 → 🔗 按钮错误地打开了 modalWorkflow（API文档面板）而非 modalComfyUI（配置弹窗）

### 配置
- ComfyUI 服务器: http://127.0.0.1:8002
- PromptKit 服务器: http://127.0.0.1:8080
- 活动工作流: Flux文生图 (Flux.2 Dev FP8), 4 steps
- 种子词条: 165 条（5 模块: camera_move, color, tone, storyboard, seedance）

### 需要注意
- 模块预设存储在数据库 config 表 key='module_presets'
- ComfyUI 配置存储在数据库 config 表 key='comfyui_config'
- 前端 🔗 按钮现在打开 modalComfyUI（标题含 "模块预设"）
- 批量 AI 生成是逐张串行（避免 ComfyUI OOM），大数量时需耐心等待
