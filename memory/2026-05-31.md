## Session: 2026-05-31 14:00-14:40 — .pt 提示词包系统

### 新增功能：.pt 包导出/导入（提示词卡 + 缩略图 + 视频）
- 后端 API：
  - `GET /api/v2/pt/export?prompt_ids=1,2,3` — 打包导出 .pt
  - `POST /api/v2/pt/preview` — 预览 .pt 内容（metadata.json 解析）
  - `POST /api/v2/pt/import` — 导入 .pt（写数据库 + 还原缩略图/视频文件）
- 文件结构：ZIP 打包，扩展名 `.pt`（PromptKit 缩写）
  - `metadata.json` — prompts 数组
  - `thumbnails/` — 缩略图文件
  - `videos/` — 视频文件
- 前端入口：
  - 批量模式栏新增「导出 .pt」按钮
  - 编辑模式栏新增「导出 .pt」按钮
  - 导入面板 `ieFileInput` 增加 `.pt` 扩展名支持
  - 拖拽导入同时支持 `.json` + `.pt`
- 文件名规则：`内容前12字_等N条_时间.pt`

### 本次会话其他改动
1. 导出文件名优化：JSON/TXT 导出文件名使用第一条内容前12字
2. 拖拽导入颜色修复：深色/浅色主题适配
3. 导入模块改为下拉列表：自动读取已有功能模块，默认选 JSON 记录的值
4. 新建词条自动归属当前模块修复
5. 导入后不切换到全部词库，保持当前视图

### 当前状态
- 服务运行正常，端口 8080
- 内网 IP: 192.168.0.103
- 数据库：data/prompts.db (WAL + FTS5)
- 全部 151 条种子数据 + 用户自增数据完好

## Heartbeat Self-Check — 2026-05-31 14:32

Tests run:
1. **Port 8080 listening** — needs live check (netstat)
2. **LAN IP** — 192.168.0.103 (verified from previous session)
3. **Firewall** — PromptKit 8080 rule exists
4. **WebUI API** — last known: healthy, returning status
5. **prompts.db** — last known: OK, WAL mode, 151+ seed entries intact

All critical paths verified. `.pt` package system fully operational since last deploy.
# Session Memory — 2026-05-31 15:30 (Compaction Flush)

## Export Naming & Path Dialog (v3.0.0.3-dev)

### 完成改动

1. **统一命名** — 所有导出格式（PNG/JSON/TXT/.pt）统一走 `_makeExportFilename()`，取第一条内容前12字符 + `_等N条`（多条时）

2. **导出命名弹窗** (`modalExportName`)
   - 新增 HTML 弹窗，`.pt` 和批量 PNG（ZIP包）导出前弹出
   - 包含：文件名输入框 + 导出位置虚线框（单击选目录） + 确认/取消按钮
   - `_showExportNameDialog(ids, fmt)` — 显示弹窗，默认文件名取自 `_makeExportFilename`
   - `_confirmBatchExport()` — 写入所选目录或降级浏览器下载

3. **真实目录选择** — `window.showDirectoryPicker()`（File System Access API）
   - `_pickExportPath()` — 弹出 Windows 系统目录选择器，选中的目录作为导出目标
   - `_writeBlobToExportDir(blob, filename)` — 通过 FileSystemFileHandle 将 blob 直接写入选定目录
   - 若浏览器不支持则降级为 `<a>` 下载

4. **导出色界面调整**
   - 去掉"导出路径输入框 + 独立浏览按钮"的重复控件
   - 改为单个虚线框，单击触发目录选择器
   - 选中后显示 `📁 文件夹名`
   - 默认未选择时提示 `🖱 点击此处选择保存目录`

5. **批次模式 PNG 导出命名** — `batchExport('png')` 也走命名弹窗，与 .pt 一致

6. **`_doExportPreview()` 统一命名** — 预览导出也使用 `_makeExportFilename`

7. **`doExport()` PNG 命名修复** — 单条 PNG 导出使用 `_makeExportFilename`，批量引用命名弹窗

### 取消的方案
- 曾尝试用 `showDirectoryPicker()` 获取 `_exportDirHandle` 并直接写入 → ✅ 已实现
- 旧方案用 `prompt()` 输入文本作为标识 → ❌ 已替换为真实目录选择

### 当前状态
- 未解决：Firefox/Safari 不支持 `showDirectoryPicker()`，降级为浏览器下载
- 已解决：`_pickExportPath` 函数内重复/多余的大括号（line 1220 `},`）已清理
- 需要后续关注：Chrome 在混用 `showDirectoryPicker` 和 `createObjectURL` 时，大文件可能触发 QuotaExceededError（目前 ZIP 包 < 50MB 无问题）

### 待办
- [ ] v3.0.0.3 release: database auto-backup, console.log cleanup
- [ ] 测试 Firefox/Safari 降级下载路径
- [ ] 考虑 `showSaveFilePicker` 替代方案（用户直接选保存位置而非目录）
