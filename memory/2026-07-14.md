# 会话记忆 — 2026-07-14（Presence + 审计 + Phase35 工作空间/MAM）

## 本会话完成的四大块（均已端到端验证）

### 1. Phase34 实时在线状态（Presence）
- 新增 `backend/presence.py`（独立 `/ws/presence?token=JWT` 通道，状态机 online/idle/away/busy/offline，多设备聚合，15s 巡检广播）+ `frontend/static/js/presence_client.js`（登录即连、心跳、header 在线指示器）。
- main.py 注册 router + lifespan 起 `presence_sweep_loop()`。admin_users.js/auth_client.js/index.html 已接。
- 验证：多账户 WS 11/11 PASS；状态机单测 OK。回归：`python backend\_test_presence.py`（需 websockets 库）。

### 2. Phase35-audit 用户活动审计日志
- 新增 `backend/audit.py`（权威表 `user_audit_log`；`record_audit()`；admin-only 端点 `/api/audit/user/{uid}` `/actions` `/sessions` `/summary` `/feed` `/event-types`）。
- 埋点：auth.py(login/login_failed/logout/register)、api/users.py(update/delete/toggle)。action_logger.record_action + logs.py 补 actor_id。
- 关键修正：`/user/{uid}` 按 `(user_id=? OR (target_type='user' AND target_id=?))`，让管理员对该账户的操作也出现在该账户日志里。
- 前端 admin_users.js v2(?v=2)：用户卡「📜 日志」按钮 → 弹窗(概览chips + 审计/行为/会话三标签 + 搜索/筛选/分页)。
- 验证：18/18 PASS。回归：`python backend\_test_audit.py`。
- 遗留：历史 user_actions(2219条) actor_id 仍 NULL；旧登录无审计记录。

### 3. Phase35 工作空间 — 用户已确认的决策（重要）
- **Q1（关键）**：混合方案。服务器主盘=关键信息/索引 + 共享空间；各自设备盘=各自媒体素材(图/视频/音频)+工程源文件(C4D/AE/PS)。
- Q2：每人可在自己工作空间内建多个项目空间分别管理。
- Q3：主盘负责索引/查重/检测/恢复关键信息；本地设备盘管制作中媒体+工程文件；两者结合。
- Q4/Q5/Q6/Q7：通过（旧数据归默认公共空间；先私有/公共两级；预设后续完善；先后端跑通）。
- **分级存储策略（已同意「按你推荐」）**：L0索引层(服务器·全部指纹/元数据/缩略图) / L1备份层(服务器·关键资产真实副本，内容寻址去重) / L2本地层(设备盘·制作中文件只索引不全备)。**核心认知：索引≠备份，防丢必须存真实字节**。
- 方案文档：`PLAN_Phase35_user_workspace.md` + `PLAN_Phase35_MAM.md`（竞品对比 Connecter/Perforce P4 DAM/Anchorpoint/Kitsu/ShotGrid/Frame.io/immich；差异化=AIGC流程内嵌+项目内模块化隔离+本地重资产/服务器轻索引+团队版本审核+自愈备份）。

### 4. Phase35.0/35.1 已落地（后端+前端，均验证）
- **35.0 迁移** `migrate_phase35.py`：新表 user_workspace/folder_preset/project_space/asset_catalog + master_project 加可空 workspace_id 回填默认公共空间(id=1) + 系统 7 段预设。备份 data/backups/phase35_pre_*.db。FK=0。
- **35.1 迁移** `migrate_phase35_1.py`：asset_module 字典(14模块) + project_space +modules_json/backup_policy + asset_catalog +module_key/is_critical/backup_status/backup_path。备份 phase35_1_pre_*.db。
- **35.1 API** `backend/api/asset_library.py`：模块字典；项目 CRUD（建项选模块→生成隔离目录，私有自动建个人ws，共享进默认公共ws）；资产上传(sha256查重+缩略图[PIL图/ffmpeg视频首帧]+入库，工程文件默认 is_critical=1)/列表/缩略图·原文件服务(限 data/ 防穿越)/删除/查重报告；私有隔离 _can_access；审计埋点 project_create/delete、asset_upload/delete。存储：私有 data/workspaces/user{uid}/projects/proj{pid}/；共享 data/projects/proj{pid}/；缩略图 data/catalog_thumbs/{cid}.jpg。验证 20/20。回归 `python backend\_test_asset_library.py`。
- **35.1-UI** `frontend/static/js/asset_library_ui.js?v=1`（自包含 PK_ASSETLIB，不侵入项目插件）+ index.html #viewProjectAssets 面板。header「📦 项目资产」→ 项目列表(全部/我的/共享) → 新建向导(名称/可见性/备份/多选模块) → 项目详情(按模块分区网格,缩略图/星标关键/下载/删除,多文件上传,重复告警,⚙编辑模块,🔍查重)。JS node --check 通过 / 服务200。

## 部署/运行状态
- **reload=False**，本会话多次重启 8080。最后一次工作服务：`python backend\main.py` 后台（Presence 阶段日志 backend/_presence_startup.log；审计 PID 曾 45680；资产库 PID 曾 44716）。静态资源改动免重启，用户强刷即可。
- 本机 LAN IP 192.168.0.101；防火墙 8080 自检提示未显式放行（旧提示，LAN 可访问，不影响）。
- 关键约束（AGENTS/SOUL）：纯局域网、不上云、不做公网穿透/端口映射、Windows only、数据本地。

## 待办 / 下一阶段
- **下一步（用户选先做 35.1 UI，已完成）→ 待用户实际点测后进 35.2**。
- **35.2**：asset_version + asset_review + 状态流(draft→in_review→approved/rejected) + 团队共享(接 project_members/角色)。
- **35.3（自愈备份核心）**：设备盘索引机制（M-Agent 推荐 / M-FSA 需局域网 HTTPS 自签，仅 Chromium）+ 完整性自检(指纹比对缺失/损坏) + 关键资产内容寻址备份 + 恢复到新设备。
- **35.4 进阶**：DCC 集成(C4D/PS/AE 拖拽导入导出)、AI 标签、3D 预览、PSD/3D 手绘批注审阅、批量重命名。
- 可选增强（用户曾问，未做）：Presence 显示当前所在页面/项目、admin 强制下线；审计日志导出 CSV/保留期清理/可疑登录告警。
- 遗留待接：项目/素材 create/update/delete 审计已定义 event_type，asset_library 已埋 project/asset，但 master_project(旧团队项目) 业务端点仍未埋审计。
- 临时脚本（可清理）：backend/_inspect_*.py。

## 导航重组（2026-07-14 13:28）
- 顶部导航新增静态「项目」下拉 `navDropdownProject`(bi-kanban)，含：项目看板(`PK.open()`) / 项目资产库(`PK_ASSETLIB.open()`) / 媒体资产(`App.switchView('v4media')`)。
- 从「管理」下拉移除「媒体资产」(v4media) 项（已移入「项目」下拉）。
- asset_library_ui.js 停用独立 📦 头部按钮（init 改调 `_navFix`），并隐藏项目插件在 pluginNavRight 的重复「项目管理」按钮(.bi-kanban)。版本 ?v=3。
- 纯前端改动免重启，强刷生效。

## 历史媒体数据迁移（2026-07-14 13:50）
- 脚本 backend/migrate_legacy_media.py（幂等，执行前 VACUUM 快照 legacy_media_pre_*.db）：将旧 media_assets(473) 在位索引进「媒体资产库」(Phase35)，不搬动/不复制原文件。
- 目标：默认公共工作空间(id=1)下新建共享项目「历史媒体归档」(pid=6, shared, owner=admin, modules=image/video/audio, backup_policy=none)。
- 在位索引：asset_catalog.local_rel_path 存相对项目根的 ../../originals|thumbnails|videos/<file>；asset_file 端点 join 后落在 DATA_DIR 内→过防穿越校验。图片复用已有缩略图；视频用 ffmpeg 生成缩略图到 catalog_thumbs/{cid}.jpg。指纹 sha256(>80MB 用 sz:size:name)。每资产建 v1 版本，review_status=approved。
- 结果：已迁移 398(371图+27视频)；75 条无文件死记录跳过(source=wordcard_thumb/video，原文件已不存)。回退搜索含 wordcard_thumbs/videos/wc_media。FK=0。验证：归档项目可见，图/视频缩略图+原文件均可服务，纯数据免重启。旧 v4media 面板/media_assets 表未动。


## 词包→收藏 彻底归一（2026-07-14 14:30）
- 分析：自定义词包(wordpacks) 与 词卡收藏(collections) 功能重复（同为用户自定义词卡分组、同在 app_collections.js、同用 .collection-card）；wordpacks 几乎未用(1包0条)，collections 在用(9/12)且有封面。已从导航移除「自定义词包」项。
- 本次把批量工具栏「添加到词包」彻底换为「添加到收藏」：
  - 后端 v2.py 新增 POST /api/v2/collections/{cid}/items/batch-add（收 prompt_ids，INSERT OR IGNORE，返回 added）。
  - 前端 app_tools.js(?v=9.5) 新增 batchAddToCollection()/doAddToCollection()（复用原 modalAddToWordpack 弹窗 DOM，列 collections）。
  - index.html 批量栏按钮改：onclick=App.batchAddToCollection() id=btnBatchAddColl 图标 bi-bookmark-plus 文案「添加到收藏」。旧 batchAddToWordpack/doAddToWordpack 保留为死代码(无触发)。
- 验证：重启后 batch-add 端到端 PASS（收藏#4 4→6 added=2，清理回4）；JS node --check 通过；无 JS 按旧按钮 id 控显隐。当前服务日志 backend/_p_batch.log。

## 本会话导航/命名调整汇总（2026-07-14 下午，均纯前端强刷生效）
- 「管理」下拉→改名「词库」；移除其中「媒体资产」「自定义词包」两项。
- 新增「项目」下拉：项目看板(PK.open) + 项目资产(PK_ASSETLIB，原名项目资产库/媒体资产库，最终定名「项目资产」)。旧 v4media「媒体资产」入口去冗余（数据已迁入「历史媒体归档」项目）。
- 在线状态指示器移到用户按钮后、去文字、统一图标(bi-people-fill 颜色表状态+人数角标)。presence_client.js?v=2。
- 收藏夹→词卡收藏；回收站→词卡回收；LLM Playground/提示词优化引擎→词卡优化；数据库备份→数据备份；ComfyUI配置→扩展工具(bi-puzzle)；统计仪表盘合并进服务监测(监测弹窗头部加📊统计按钮，monitor_dashboard.js?v=6)。
- 品牌名点击→App.switchAllGroups()（跳词库并显示全部分组总览页）。
- 注意：index.html 是 UTF-8 BOM + 混合 CRLF/LF 行尾，edit 工具多行匹配易失败，改用 Python 脚本按唯一子串增删改最稳。


## 资产入口彻底归一 + 项目资产分库（2026-07-14 14:50）
### 分析结论
- 导航右侧「资产管理」按钮 = 独立插件 com.promptkit.asset「资产管理专业版 v1.0」(navAssetMgmt/PK_AssetManager)，主表 project_assets(1行)、asset_versions/ratings/tags/prompt_ref/duplicates 全0 → 基本闲置。与 Phase35「项目资产」(PK_ASSETLIB) 高度重叠(上传/去重/版本/画廊)。独有点：生成参数溯源 + 提示词↔词卡关联(待并入项目资产)。
- 右侧「项目管理」按钮(navProjectMgmt) 与「项目」下拉的「项目看板」重复。
### 执行(方案A，已重启2次)
- 停用资产管理专业版：plugins/asset → plugins/_disabled/asset（发现逻辑跳过 _ 开头目录，确认停用；数据表未删，可回退）。
- 移除项目管理右侧按钮：**关键**——nav_buttons 是 plugins/project/__init__.py 的 get_frontend_injections() 硬编码（非 plugin.json！），已改为 nav_buttons:[]，保留 styles/scripts 使 project_dashboard.js 仍注入、PK.open 仍可用。plugin.json 的 nav_buttons 也顺手清空(无实际作用)。
- 结果：manifest injections.nav_buttons=0；pluginNavRight 空；全部资产入口统一到顶部「项目」下拉(项目看板/项目资产)。
### 项目资产分库(asset_library_ui.js?v=7)
- renderList 改为两个库分区：🌐 共享公资产库(visibility shared/public 或被共享给我的私有) + 🔒 用户私人资产库(owner==我 且 private)，各带计数与空态。抽出 _projCard 复用。去掉旧 全部/我的/共享 scope 按钮。
- 后端 visibility(private/shared) 模型已支持，纯前端分组；scope=all 拉取后按 owner+visibility 客户端切分。
### 现服务 PID 41160。待办：把「生成参数溯源/提示词↔词卡关联」并入项目资产(可选)。


## 生成溯源 + 提示词/词卡关联 并入项目资产（2026-07-14 15:00）
- 迁移 migrate_phase35_prov.py：asset_catalog += gen_prompt/gen_model/gen_params_json/rating；新表 asset_catalog_ref(catalog_id,ref_type[word_card|prompt|shot],ref_id, UNIQUE)。快照 prov_pre_*.db，FK=0。
- 后端：asset_library.update_asset(PATCH) 扩展支持 gen_prompt/gen_model/gen_params(dict→json)/rating(0-5)。asset_review.py 加 import json + 富化 asset_detail(gen_params 解析 + refs) + 新端点 GET /api/assets/{cid}/refs、POST /link{ref_type,ref_id}、DELETE /link/{ref_type}/{ref_id}；_resolve_refs 用 word_card.id 解析名称；写操作需 owner|editor。audit EVENT_DICT +asset_link。
- 前端 asset_library_ui.js?v=8：资产详情弹窗新增两区——🧬生成溯源(5星评分即时保存 setRating / 模型+Prompt+参数JSON 保存 saveProvenance) + 🔗关联词卡(chips 展示+移除 unlinkRef / 搜索 /api/v4/cards?search= 点选 linkRef)。
- 验证：重启后端到端 8/8 PASS（PATCH溯源+评分回读、gen_params.seed、link/refs/unlink、名称解析）。服务 PID 14352。
- 至此「资产管理专业版」的价值特性已全部并入项目资产，该插件彻底退役无遗漏。


## Phase36 角色工作流 — 决策确认 + 角色词卡分组种子（2026-07-14 16:20）
### 已确认决策
- Q1：区分「公共角色模版/定义」与「项目角色实例」。总项目角色**继承复用**公共角色库模版，可在其上改并做**独立历史版本管理**，含完整角色档案（参考图/三视图/资料），并**整合分镜组装器里的角色定义功能**。→ 采用「模版层 + 项目实例层(copy-on-adopt + 独立版本)」而非简单多对多选取。
- Q2：**弃用** master_asset(character) 冗余，项目看板角色页统一改用 character_profiles 体系。
- Q3：模版走深度(b)（字段 + 每槽绑定词卡分组）；先落地「角色构成词卡分组」。
### 已执行：角色构成词卡分组种子（seed_character_groups.py，幂等+快照 charseed_pre_*.db）
- 词库新增根组「🎭 角色设定」(group_key=char_root, id=94) + 14 子分组：性别年龄/发型发色/脸型五官/表情神态/体型身材/服装服饰/配饰道具/姿态动作/画风风格/光照氛围/色调质感/职业身份/气质性格/背景场景。
- 共 110 张中英词卡（name中/content英/content_zh中/media_type=image/card_role=component/is_builtin=1），FTS 触发器自动同步(1061→1171)。
- 验证：/api/v4/word-cards/groups/tree 返回角色设定根组含14子分组；各组词卡就位。
- 竞品/研究来源：中文AI人像提示词库(cnblogs)、四视图角色设定sheet、archetype+features+outfit+personality+style+setting 公式(zsky/promptomania)、发型/脸型专业分析。
### 待办（Phase36 建设，按确认架构）
- 36.1 character_template 模版库(structure_json 槽位↔词卡分组绑定) + 组装器接模版；36.2 项目角色实例(继承公共模版+独立版本 asset 化+档案三视图) + 弃用 master_asset(character)；36.3 分镜组装器读本总项目角色 + 整合角色定义功能。
- 方案文档 PLAN_Phase36_character_workflow.md。


## Phase36.1 角色设定模版库 + 组装器接模版（2026-07-14 16:45）
- 迁移 migrate_phase36.py（快照 phase36_pre_*.db，FK=0）：新表 character_template(name/description/structure_json[槽位:key,label,icon,group_keys,order]/default_settings_json/is_builtin/sort_order) + character_profiles += template_id。种子 4 内置模版：通用角色设定 / 日系动漫少女 / 赛博朋克角色 / 奇幻冒险者（结构=17槽位，默认值复用旧 PRESET）。
- 后端 api/character_composer.py：CHARACTER_DIMENSIONS **重绑到 char_ 角色分组**（gender/age→char_gender_age、hairstyle→char_hair、facial→char_face、expression→char_expression、body→char_body(新)、clothing→char_clothing、accessory→char_accessory(新)、pose→char_pose、occupation→char_occupation(新)、temperament→char_temperament(新)、style→char_style、background→char_background、lighting→char_lighting、color_scheme+quality→char_color、negative→negative）共17维；/dimensions?template_id= 与 /dimensions/{k}/cards?template_id= 支持按模版槽位解析(_template_slots/_resolve_groups)；/compose 扩展纳入 occupation/body/temperament/accessory；create/update 存 template_id；新增 /templates CRUD(GET列表/GET{id}/POST/PUT/DELETE，内置不可删)。
- 前端 character_composer.js?v=5：角色列表底部「预设模板」→ 改为数据驱动「🎭 角色设定模版」列表(loadTemplates/newFromTemplate：按模版 default_settings + template_id 新建角色)；维度重绑后每槽「选词库」下拉即拉取对应 char_ 角色分组词卡。
- 验证：后端 10/10（17维/绑定char组有卡/新维度/expression卡片/模版数≥4/结构17槽/按模版取维度）；按模版新建往返 template_id持久化+compose文本正确。服务 PID 45484。
- 待办：36.2 项目角色实例(继承公共模版+独立版本+档案三视图)+弃用master_asset(character)；36.3 分镜按项目取角色。自定义模版目前经 API POST /templates；组装器内「可视化建模版UI」为后续可选。


## Phase36.1-scene 场景设定模版化（对齐角色改造）（2026-07-14 17:00）
- 场景词卡分组种子 seed_scene_groups.py（幂等+快照 sceneseed_pre_*.db）：词库新增根组「🏞 场景设定」(scene_root,id=109) + 13 子分组(scene_location/architecture/time/season/weather/atmosphere/lighting/color/perspective/composition/details/style/quality)，共 102 张中英词卡；FTS 触发器同步(1171→1273)。
- 迁移 migrate_phase36_scene.py：scene_template 表(结构14槽绑定 scene_ 分组 + default_settings) + scene_profiles += template_id；种子 6 内置模版(通用场景设定 + 赛博朋克都市/魔法森林秘境/远古神殿遗迹/海滨日落/冬日雪村)。
- 后端 api/scene_composer.py：SCENE_DIMENSIONS 14维重绑 scene_ 分组；/dimensions?template_id= 与新增 /dimensions/{k}/cards?template_id= 模版感知(_resolve_groups/_template_slots)；create/update 存 template_id；新增 /templates CRUD。compose 键不变无需改。
- 前端 scene_composer.js?v=7：预设区改数据驱动「🏞 场景设定模版」列表(loadTemplates/newFromTemplate)；维度重绑后每槽拉取对应 scene_ 分组词卡。
- 验证：后端 9/9（14维/绑定scene组有卡/weather卡片/模版≥6/结构14槽/按模版取维度/template_id持久化/compose）。compose 示例「吉卜力动画风格，古老魔法森林深处，精灵风格树屋…」。服务 PID 45380。
- 链路完整：词卡→场景组装(公共场景模版)→场景实例(scene_profiles)→分镜(scene_composer 已有 apply-to-shot：scene→user_project_scene 字段映射 + scene_profile_id 外键)。
- 待办同角色：36.2 项目场景实例(继承公共模版+独立版本)+总项目场景库；场景「可视化建模版UI」可选。


## Phase36.2 总项目 角色/场景 实例（继承+版本+档案）后端（2026-07-14 18:30）
- 迁移 migrate_phase36_2.py（快照 phase36_2_pre_*.db，FK=0）：统一三表 project_role(master_project_id/role_type[character|scene]/name/settings_json/source_profile_id/source_kind/template_id/current_version_id/version_count/review_status/cover_image/notes) + project_role_version(version_no/settings_json/name/note/author) + project_role_asset(asset_kind[ref_image|three_view|turnaround|material|other]/rel_path/thumb_path/caption)。一套服务角色+场景。
- 后端 api/project_roles.py（自持sqlite+get_current_user）：
  - 列表/详情：GET /api/master/{mid}/roles?role_type= ; GET /api/roles/{rid}(含versions/assets/血缘source_name)
  - 继承/新建：POST /api/master/{mid}/roles/adopt{role_type,source_profile_id}(从 character_profiles/scene_profiles 复制settings→实例+快照v1，记 source_profile_id/source_kind血缘) ; POST /api/master/{mid}/roles(新建空实例)
  - 编辑自动版本：PUT /api/roles/{rid}(settings变更→_snapshot新版本 version_count+1) ; GET /versions ; POST /rollback/{vid} ; DELETE
  - 档案：POST /api/roles/{rid}/assets(multipart 上传参考图/三视图,PIL缩略图,首图设封面) / GET assets / DELETE /api/roles/assets/{aid} / GET .../file|thumb / GET /api/roles/{rid}/cover
  - 存储 data/role_assets/role{id}/ ; 缩略图 data/role_thumbs/{aid}.jpg ; 路径限 DATA_DIR 防穿越。main.py 注册 project_roles_router。
- 验证（重启后 16/16）：继承创建/settings继承/血缘source_name/version_count=1/编辑触发新版本/版本数2/回滚v1/回滚后还原/上传三视图+缩略图/档案列表/封面服务/项目角色库含实例/新建场景实例/场景库/删除404。服务 PID 44080。
- 待办 36.2-d 前端：项目角色/场景库 UI（继承选取公共库/编辑/版本时间线/回滚/档案三视图上传）——落点待定：项目看板(P2, project_dashboard.js 78KB插件)内嵌 或 自包含面板(如 asset_library_ui 模式)。旧 master_asset(character/scene) 待此UI上线后正式弃用。
- 说明：master_project 现有2个(id=1,2)，无 owner，团队共享；实例 owner_user_id 记录创建者。


## Phase36.2-d 前端：自包含「项目角色/场景库」面板（方案2）（2026-07-14 18:40）
- 后端补 GET /api/master-projects（总项目列表+char/scene计数，供 UI 先选总项目）。
- 新增 frontend/static/js/project_roles_ui.js?v=1（PK_ROLES，自包含，不动项目插件）+ index.html #viewProjectRoles 面板 + 导航「项目」下拉加「👥 项目角色库」。
- 流程：项目「项目角色库」→ 选总项目(卡片显角色/场景计数) → 角色库/场景库 tab → 「⬇从公共库继承」(列 character_profiles/scene_profiles 点选 adopt) / 「＋新建」 → 实例卡(封面/审核徽章/v版本数·档案数)。
- 实例详情弹窗 openInstance：设定字段编辑(key→输入,LABELS中文映射,＋字段) + 💾保存(变更→自动新版本) + 📦档案(参考图/三视图/转身/资料 分区上传缩略图/删除,首图设封面) + 🕘版本历史(回滚) + 删除实例。走全局 fetch 拦截器带 token；开面板即折叠侧边栏。
- 验证：JS node --check 通过 / 服务200 / /api/master-projects 返回2项目 / 含 openInstance+doAdopt；后端 36.2 端到端 16/16。服务 PID 45720。
- 至此 Phase36 链路闭环：词卡→(角色/场景)公共模版库→组装器接模版→总项目继承为独立实例(版本+档案三视图)→分镜(scene apply-to-shot 已通；角色分镜取数待36.3)。
- 待办 36.3：分镜组装器按「本总项目」取角色/场景实例(现取全局)；旧 master_asset(character/scene) 可正式弃用(数据保留)。


## Phase36.3 分镜按「本总项目」取角色/场景 + apply-to-shot（2026-07-14 18:55）
- 关联：master_sub_project(seedance_project_id↔master_project_id) 解析分镜项目所属总项目。
- 后端 api/project_roles.py 新增：GET /api/seedance/{spid}/roles?role_type=(解析master_of_seedance→返回该总项目 project_role 实例) ; POST /api/roles/{rid}/apply-to-shot{shot_id}(将实例settings映射写入 user_project_scene：character→_apply_character 组 subject/emotion/action ; scene→_apply_scene 映射 scene_desc/emotion/lighting/weather/color_grade/perspective/composition/environment_detail/filter，镜像旧 scene_composer)。
- 前端：project_roles_ui.js?v=2 加 PK_ROLES.shotApply(shotId,roleType)/doShotApply（用 App.seedanceV2.currentProjectId 取spid→选实例→apply→openProject 重渲染）；seedance_v2_composer.js?v=91 镜头卡「角色/场景」行加「📌角 / 📌景」按钮。
- 验证：后端 4/4（建实例/seedance解析master并列实例/apply-to-shot/镜头subject更新含"黑长直"）。服务 PID 47964。
- **Phase36 链路全闭环**：词卡→(角色/场景)公共模版库(char_/scene_分组+14/13维)→组装器接模版拼装→character_profiles/scene_profiles(公共)→总项目继承为 project_role 独立实例(自动版本+回滚+档案三视图/参考图)→分镜镜头 apply(本总项目实例→user_project_scene字段)。
- 旧 master_asset(character/scene) 正式被 project_role 取代（数据保留，未删）。可选后续：分镜「本项目」按钮与旧全局角色/场景选择器合并统一；project_role 审核流(review_status 已留列)。


## Phase36 可选优化：分镜统一选择器 + 实例审核流（2026-07-14 19:05）
### 优化① 分镜镜头「角色/场景」统一选择器
- seedance_v2_composer.js?v=92：镜头卡「角色」「场景」两个 selector 的 onclick 改为 PK_ROLES.shotApply(shotId,'character'|'scene')（无 PK_ROLES 时回退旧全局 picker）；去掉之前并列的「📌角/📌景」按钮。
- project_roles_ui.js?v=3：shotApply 弹窗 = 本总项目实例(点击 apply-to-shot) + 「📚 浏览公共库」按钮(_browsePublic → 委托旧 App.characterLib.openScenePicker / App.seedanceV2._openSceneProfilePicker)。一个入口两来源。
### 优化② project_role 实例审核流
- 迁移 migrate_phase36_review.py：project_role_review 表(reviewer/action[submit|approve|reject|comment]/comment)。
- 后端 api/project_roles.py：POST /api/roles/{rid}/review(submit→in_review/approve→approved/reject→rejected + 记录 + 审计) ; GET /api/roles/{rid}/reviews ; get_role 详情含 reviews。
- 前端 project_roles_ui.js：实例详情弹窗右栏加「🛡审核」区(按状态显 提交/批准/驳回，reject 弹原因)+审核时间线；roleReview() 调用。
- 验证：审核后端 6/6（建draft/提交in_review/批准approved/详情状态/含reviews/reviews含submit+approve）。服务 PID 43960。
- 说明：project_role.review_status 状态机 draft→in_review→approved/rejected，与资产审核(asset)一致的交互风格。


## 整体回归自测 88/88 全绿（2026-07-14 19:30）
- _test_presence.py 11/11、_test_audit.py 18/18、_test_asset_library.py 20/20、_test_asset_review.py 21/21、_test_phase36.py 18/18。全部 exit=0。
- 修复：presence 测试原用 uid1(admin) 做 A，受实时浏览器在线连接干扰(connection_count/REST快照精确总数失配)→改用未在线 uid3 + 断言改子集({2,3,10}⊆ids)，抗实时客户端。非产品回归。
- 新增持久回归 _test_phase36.py（角色/场景模版+维度重绑+compose+项目实例继承/版本/审核+seedance解析master）。scene compose 断言由 len>5 改为内容包含(原"森林，晨曦"恰5字误判)。
- 结论：Phase34(在线)/35-audit(审计)/35.1(资产库)/35.2(资产审核)/36(角色场景全链路+审核) 均无回归。服务 8080 PID 43960。
