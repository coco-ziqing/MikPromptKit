# PromptKit v5.22.1 安装测试部署手册
## 目标机部署 | 2026-07-23 20:42 GMT+8

---

## 一、封装包信息

| 项目 | 值 |
|------|-----|
| 版本 | **v5.22.1** |
| 主程序 | `PromptKit.exe`（61.9MB，Windows 原生 EXE） |
| 完整包 | `PromptKit/` 文件夹（约 170MB，含 Python 运行时） |
| 源机 IP | `192.168.0.102:8080` |
| 目标系统 | **仅 Windows 10/11（64 位）** |
| 目标机无需安装 | Python / Node.js / 数据库 均不需要 |
| 默认端口 | **8080** |
| 默认账户 | 用户名 `admin` 密码 `admin` |

### 目录结构

```
PromptKit/                       ← 把整个文件夹复制到目标机任意位置
├── PromptKit.exe                ← 双击启动
├── _internal/                   ← 运行时环境（不可手动修改）
│   ├── frontend/                ← Web 前端页面
│   │   ├── index.html           ← 主页面
│   │   ├── cover_editor.html    ← 封面编辑页
│   │   └── static/
│   │       ├── css/style.css
│   │       ├── js/              ← 前端脚本
│   │       ├── img/covers/      ← 封面图片 (3张)
│   │       └── keygen.html      ← 激活码生成器
│   ├── plugins/                 ← 插件（AI 生成中心 + 项目管理）
│   ├── VERSION                  ← v5.22.1
│   └── ...                      ← Python 运行时 + 系统 DLL
└── data/                        ← 用户数据目录
    ├── prompts.db               ← SQLite 数据库（核心数据，10.2MB）
    ├── .jwt_secret              ← JWT 签名密钥（勿泄露）
    ├── .license_seed            ← 许可密钥种子（勿泄露）
    ├── licenses/                ← 激活许可文件
    ├── workspaces/              ← 项目文件存储
    ├── catalog_thumbs/          ← 资产缩略图
    └── ...
```

---

## 二、第一步：将包复制到目标机

**任选一种方式：**

```
方式A（U 盘/移动硬盘）：
  把源机的 PromptKit\ 整个文件夹复制到 U 盘 → 目标机粘贴到任意位置

方式B（局域网共享）：
  目标机打开资源管理器 → 地址栏输入 \\192.168.0.102 → 定位到 PromptKit 目录 → 复制到本地

方式C（打包 ZIP）：
  右键 PromptKit\ → 发送到 → 压缩(zipped)文件夹 → 传到目标机 → 解压
```

**推荐放置路径**：`D:\PromptKit\`（英文路径，避免空格和中文）

---

## 三、第二步：防火墙放行（管理员操作，只做一次）

以**管理员身份**打开 PowerShell 或命令提示符，执行以下命令：

```powershell
# 放行 8080 端口 TCP 入站连接
New-NetFirewallRule -DisplayName "PromptKit-8080" -Direction Inbound -Protocol TCP -LocalPort 8080 -Action Allow -Profile Any
```

验证规则已创建：

```powershell
Get-NetFirewallRule -DisplayName "PromptKit-8080" | Select-Object DisplayName, Enabled, Direction, Action
```

> **注意**：如果 8080 端口已被占用，EXE 启动时会自动探测 8081、8082…，需重复放行对应端口。

---

## 四、第三步：启动服务

```
双击 D:\PromptKit\PromptKit.exe
```

首次启动会弹出一个黑色控制台窗口，显示启动日志。等待约 **20-30 秒**，直到看到：

```
==================================================
  [OK] 咪卡MiK提示词助手 v5.22.1 已启动
  [本机] http://127.0.0.1:8080
  [局域网] http://192.168.0.xxx:8080
  [词库] 169 条 | 卡片 165 | 资产 0
==================================================
```

> ⚠️ **控制台窗口不能关闭**，关闭即停止服务。

---

## 五、第四步：六大验证清单

### 验证 1：本机浏览器基本连通

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1.1 | 打开浏览器，输入 `http://127.0.0.1:8080` | 看到登录封面页 |
| 1.2 | 输入 `admin` / `admin` 登录 | 进入词库首页 |
| 1.3 | 左上角品牌名显示版本号 | `v5.22.1` |

### 验证 2：局域网跨设备访问

| 步骤 | 操作 | 预期 |
|------|------|------|
| 2.1 | 手机/平板/另一台电脑连接**同一 WiFi** | |
| 2.2 | 浏览器输入控制台显示的局域网地址 | 看到封面页 |
| 2.3 | 登录 `admin` / `admin` | 进入词库首页 |

### 验证 3：词库功能

| 步骤 | 操作 | 预期 |
|------|------|------|
| 3.1 | 左侧边栏显示词库分组列表 | 全部折叠状态 |
| 3.2 | 点击「🎭 角色设定」名称 | 右侧弹出子分组卡片网格 |
| 3.3 | 点击任一子分组卡片 | 右侧显示词卡列表 |
| 3.4 | 点击「🔤」切换到「🧠」语义搜索 | 输入关键词后显示搜索结果 |
| 3.5 | 点击左侧三角箭头展开/折叠 | 仅折叠功能，不触发浏览器 |
| 3.6 | 点击返回按钮 | 智能返回父组浏览器 |

### 验证 4：版本切换 + 激活系统

| 步骤 | 操作 | 预期 |
|------|------|------|
| 4.1 | 品牌右侧显示 📚📦👥 三按钮 | 默认 📚 高亮 |
| 4.2 | 点击「📦 个人项目」 | 弹出激活对话框 |
| 4.3 | 弹窗中显示本机指纹 + 复制指纹按钮 | 点击可复制 |
| 4.4 | 点击「📋 打开激活码生成器」 | 新标签页打开生成器 |
| 4.5 | 输入 `admin` / `admin` 后再次点击「📚」 | 回到词库首页 |

### 验证 5：封面编辑

| 步骤 | 操作 | 预期 |
|------|------|------|
| 5.1 | 导航栏「工具」→「封面编辑」 | 打开编辑页 |
| 5.2 | 封面显示 3 个素材位 | 含文字说明 |
| 5.3 | 编辑标题/副标题后点「💾 保存」 | 保存成功提示 |
| 5.4 | 退出登录后重新查看封面页 | 封面已更新 |

### 验证 6：个人详情头像

| 步骤 | 操作 | 预期 |
|------|------|------|
| 6.1 | 右上角头像 → 个人详情 | 弹出详情页 |
| 6.2 | 点击「📷 上传头像」 | 可选择本地图片 |
| 6.3 | 修改显示名称 → 「💾 保存资料」 | 保存成功 |

---

## 六、高级验证（可选）

### 项目看板

| 步骤 | 操作 | 预期 |
|------|------|------|
| A1 | 导航栏「项目」→「项目看板」 | 打开看板 |
| A2 | 点击「📦 创建总项目」→ 输入名称→ 保存 | 侧边栏出现新项目 |
| A3 | 点击项目 → 右侧显示 7 阶段 | P0~P6 标签可见 |
| A4 | 项目卡片名称与编辑/删除同行 | 紧凑布局 |

### 激活码生成器

| 步骤 | 操作 | 预期 |
|------|------|------|
| B1 | 工具菜单 → 激活码生成器 | 打开生成器页面 |
| B2 | 选择版本 + 输入指纹 + 选有效期 | |
| B3 | 点击「⚡ 生成激活码」 | 显示激活码 + 可复制 |

---

## 七、常见问题排查

| 症状 | 原因 | 解决 |
|------|------|------|
| 浏览器无法打开 | 防火墙拦截 | 执行第三步放行端口 |
| 局域网设备无法访问 | 不在同一 WiFi / 防火墙 | 确认同网络；重检防火墙规则 |
| 端口被占用 | 8080 已被其他程序使用 | EXE 自动切换端口，看控制台新地址 |
| admin 登录失败 | JWT 密钥不匹配 | 删除 `data\.jwt_secret` 后重启（密码恢复为 admin） |
| 词库加载卡住 | 浏览器缓存旧 JS | Ctrl+Shift+Delete 清除缓存，然后 Ctrl+F5 强刷 |
| 杀毒软件拦截 | EXE 未签名 | 在 Windows Defender 中添加排除项 |
| 控制台中文乱码 | 系统编码问题 | 不影响功能，可忽略 |
| EXE 闪退 | 缺少 VC 运行库 | 安装 Visual C++ Redistributable 2015-2022 |

---

## 八、日常运维

### 开机自启（可选）

1. 右键 `PromptKit.exe` → 创建快捷方式
2. Win+R → `shell:startup` → 回车
3. 把快捷方式拖入该文件夹

### 数据备份

```powershell
# 定期复制 data\prompts.db 即可备份全部数据
Copy-Item "D:\PromptKit\data\prompts.db" "D:\Backup\prompts_$(Get-Date -Format 'yyyyMMdd').db"
```

### 关闭服务

直接关闭控制台窗口，或 Ctrl+C。

---

## 九、验证签字

| 验证人 | 日期 | 主机 IP | 通过项 | 遗留问题 |
|--------|------|---------|--------|---------|
| | | | /23 | |

---

**部署完成后请修改 admin 密码**：右上角头像 → 个人详情 → 修改密码。
